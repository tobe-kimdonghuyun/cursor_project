# nexacroN V24 — VideoPlayer

> 출처: `nexacro_manual.chm` 자동 추출
> 항목 수: 86개

---

## Components > Component > VideoPlayer

### VideoPlayer

> Components > Component > VideoPlayer

**Description**

비디오 파일 및 스트림을 재생할 수 있는 Wrapper 컴포넌트입니다.

**Remark**

- 오디오 파일 및 스트림을 재생할 수 있습니다.

**Property**

| Name | Description |
| --- | --- |
| accessibilityaction | VideoPlayer에 선택상자 이동 시 accessibilitydescreadtype 속성에 따라 스크린 리더의 음성 출력 대상으로 포함할 action 정보를 설정하는 속성입니다. |
| accessibilitydesclevel | VideoPlayer 에 선택상자 이동 시 하위 컴포넌트의 접근성 출력여부를 설정하는 속성입니다. |
| accessibilitydescription | VideoPlayer에 선택상자 이동 시 accessibilitydescreadtype 속성에 따라 스크린 리더의 음성 출력 대상으로 포함할 description 정보를 설정하는 속성입니다. |
| accessibilityenable | VideoPlayer 에 선택상자 이동 시 접근성 관련 속성값 출력여부를 설정하는 속성입니다. |
| accessibilitylabel | VideoPlayer에 선택상자 이동 시 accessibilitydescreadtype 속성에 따라 스크린 리더의 음성 출력 대상으로 포함할 label 정보를 설정하는 속성입니다. |
| accessibilityrole | VideoPlayer에 선택상자 이동 시 스크린 리더가 읽어주는 Role 정보를 설정하는 속성입니다. |
| aspectratio | VideoPlayer 에 로드된 비디오 파일의 화면비율값을 갖는 읽기전용 속성입니다. |
| autoplay | VideoPlayer 에 파일을 로드 시 자동으로 재생시킬 지 여부를 설정하는 속성입니다. |
| bottom | VideoPlayer 을(를) 표시하기 위한 하단 좌표값을 설정하는 속성입니다. |
| cssclass | VideoPlayer 의 Style 을 XCSS 파일에 정의된 class 로 설정하는 속성입니다. |
| currenttime | VideoPlayer 에서 비디오의 재생위치를 설정하는 속성입니다. |
| duration | VideoPlayer 에 로드된 비디오 파일의 전체 재생시간을 갖는 읽기전용 속성입니다. |
| enable | VideoPlayer 의 사용가능 여부를 설정하는 속성입니다. |
| enableevent | VideoPlayer 에서 이벤트를 발생시킬지 여부를 설정하는 속성입니다. |
| flexgrow | Layout 컨테이너 내 Main Axis(type 속성값이 "horizontal"이면 수평축, type 속성값이 "vertical"이면 수직축) 여백을 설정한 비율만큼 나누어 컴포넌트의 크기를 재조정하는 속성입니다. |
| flexshrink | Layout 컨테이너 내 Main Axis(type 속성값이 "horizontal"이면 수평축, type 속성값이 "vertical"이면 수직축)에 컴포넌트가 배치되는 영역이 컨테이너 영역을 초과하지 않도록 설정한 비율만큼 나누어 컴포넌트의 크기를 재조정하는 속성입니다. |
| height | VideoPlayer 을(를) 표시하기 위한 높이를 설정하는 속성입니다. |
| id | VideoPlayer의 고유 식별자를 설정하는 속성입니다. |
| layoutorder | Layout 컨테이너 내 Main Axis(type 속성값이 "horizontal"이면 수평축, type 속성값이 "vertical"이면 수직축)에 컴포넌트가 배치되는 순서를 설정합니다. |
| left | VideoPlayer 을(를) 표시하기 위한 좌측 좌표값을 설정하는 속성입니다. |
| maxheight | VideoPlayer 이(가) 화면에 표시되는 최대 높이를 설정하는 속성입니다. |
| maxwidth | VideoPlayer 이(가) 화면에 표시되는 최대 너비를 설정하는 속성입니다. |
| minheight | VideoPlayer 이(가) 화면에 표시되는 최소 높이를 설정하는 속성입니다. |
| minwidth | VideoPlayer 이(가) 화면에 표시되는 최소 너비를 설정하는 속성입니다. |
| mute | VideoPlayer 에서 비디오 재생 시 음소거 여부를 설정하는 속성입니다. |
| name | VideoPlayer 의 이름을 설정하는 속성입니다. |
| -nexa-border | VideoPlayer 의 테두리를 설정하는 속성입니다. |
| parent | VideoPlayer 의 부모 오브젝트를 갖는 읽기전용 속성입니다. |
| positionstep | 부모 오브젝트가 화면분할기능을 사용할 때 VideoPlayer 이(가) 표시될 화면의 인덱스를 설정하는 속성입니다. |
| repeat | VideoPlayer 에서 비디오를 반복재생할 지 여부를 설정하는 속성입니다. |
| right | VideoPlayer 를 표시하기 위한 우측 좌표값을 설정하는 속성입니다. |
| showcontrolbar | VideoPlayer 에 기본 UI(ControlBar)를 표시할 지 여부를 설정하는 속성입니다. |
| tablecellarea | Layout 컨테이너 내 가상의 Table Cell 영역 내에서 컴포넌트가 배치되는 영역을 설정합니다. |
| taborder | 탭키 입력으로 컴포넌트간 포커스를 이동할 때 VideoPlayer 의 순서를 설정하는 속성입니다. |
| tabstop | 탭키 입력으로 컴포넌트간 포커스를 이동할 때 VideoPlayer 이(가) 포커스를 받을 지 여부를 설정하는 속성입니다. |
| top | VideoPlayer 을(를) 표시하기 위한 상단 좌표값을 설정하는 속성입니다. |
| url | VideoPlayer 에 재생시킬 비디오 파일을 설정하는 속성입니다. |
| videosize | VideoPlayer 에 로드된 비디오 파일의 실제 너비와 높이를 배열형태로 갖는 읽기전용 속성입니다. |
| visible | VideoPlayer 이(가) 화면에 표시될지 여부를 설정하는 속성입니다. |
| volume | VideoPlayer 의 음량을 설정하는 속성입니다. |
| width | VideoPlayer 을(를) 표시하기 위한 너비를 설정하는 속성입니다. |

**Method**

| Name | Description |
| --- | --- |
| addEvent | VideoPlayer 에 특정 이벤트를 선언하여 추가하는 메소드입니다. |
| addEventHandler | VideoPlayer 의 특정 이벤트에 핸들러 함수를 추가하는 메소드입니다. |
| addEventHandlerLookup | 함수를 검색하여 VideoPlayer 의 특정 이벤트에 핸들러 함수로 추가하는 메소드입니다. |
| clearEventHandler | 특정 이벤트에 등록된 핸들러 함수를 모두 제거하는 메소드입니다. |
| destroy | 스크립트에서 동적으로 생성한 VideoPlayer 을(를) 삭제하는 메소드입니다. |
| findEventHandler | VideoPlayer 의 특정 이벤트에서 핸들러 함수를 찾아 인덱스를 반환하는 메소드입니다. |
| getEventHandler | VideoPlayer 의 특정 이벤트에서 지정된 인덱스에 해당하는 핸들러 함수를 반환하는 메소드입니다. |
| getOffsetBottom | 부모 컴포넌트의 Top 위치를 기준으로 VideoPlayer 의 bottom 값을 픽셀단위의 숫자로 반환하는 메소드입니다. |
| getOffsetHeight | VideoPlayer 의 높이를 픽셀단위의 숫자로 반환하는 메소드입니다. |
| getOffsetLeft | 부모 컴포넌트의 Left 위치를 기준으로 VideoPlayer 의 left 값을 픽셀단위의 숫자로 반환하는 메소드입니다. |
| getOffsetRight | 부모 컴포넌트의 Left 위치를 기준으로 VideoPlayer 의 right 값을 픽셀단위의 숫자로 반환하는 메소드입니다. |
| getOffsetTop | 부모 컴포넌트의 Top 위치를 기준으로 VideoPlayer 의 top 값을 픽셀단위의 숫자로 반환하는 메소드입니다. |
| getOffsetWidth | VideoPlayer 의 너비를 픽셀단위의 숫자로 반환하는 메소드입니다. |
| getPixelBottom | VideoPlayer 의 bottom 속성값을 픽셀단위로 반환하는 메소드입니다. |
| getPixelHeight | VideoPlayer 의 height 속성값을 픽셀단위로 반환하는 메소드입니다. |
| getPixelLeft | VideoPlayer 의 left 속성값을 픽셀단위로 반환하는 메소드입니다. |
| getPixelRight | VideoPlayer 의 right 속성값을 픽셀단위로 반환하는 메소드입니다. |
| getPixelTop | VideoPlayer 의 top 속성값을 픽셀단위로 반환하는 메소드입니다. |
| getPixelWidth | VideoPlayer 의 width 속성값을 픽셀단위로 반환하는 메소드입니다. |
| init | 스크립트로 VideoPlayer 을(를) 동적 생성한 후에 초기화하는 메소드입니다. |
| insertEventHandler | VideoPlayer 의 특정 이벤트에 핸들러 함수를 지정한 위치에 삽입하는 메소드입니다. |
| move | VideoPlayer 의 위치와 크기를 변경하는 메소드입니다. |
| pause | VideoPlayer 에서 재생 중인 비디오를 일시정지하는 메소드입니다. |
| play | VideoPlayer 에 로드된 비디오 파일을 재생하는 메소드입니다. |
| removeEvent | VideoPlayer 에 선언된 특정 이벤트를 삭제하는 메소드입니다. |
| removeEventHandler | VideoPlayer 의 특정 이벤트에서 핸들러 함수를 제거하는 메소드입니다. |
| removeEventHandlerLookup | VideoPlayer 의 특정 이벤트에서 검색된 함수와 동일한 핸들러 함수를 제거하는 메소드입니다. |
| resize | VideoPlayer 의 크기를 변경하는 메소드입니다. |
| rewind | VideoPlayer 에 로드된 비디오의 재생위치를 처음으로 설정하는 메소드입니다. |
| setEventHandler | VideoPlayer 의 특정 이벤트에서 첫번째 핸들러 함수를 변경하는 메소드입니다. |
| setEventHandlerLookup | VideoPlayer 의 특정 이벤트에서 첫번째 핸들러 함수를 검색된 함수로 변경하는 메소드입니다. |
| setOffsetBottom | 부모 컴포넌트의 Top 위치를 기준으로 VideoPlayer 의 bottom 값을 픽셀단위로 설정하는 메소드입니다. |
| setOffsetHeight | VideoPlayer 의 높이를 픽셀단위로 설정하는 메소드입니다. |
| setOffsetLeft | 부모 컴포넌트의 Left 위치를 기준으로 VideoPlayer 의 left 값을 픽셀단위로 설정하는 메소드입니다. |
| setOffsetRight | 부모 컴포넌트의 Left 위치를 기준으로 VideoPlayer 의 right 값을 픽셀단위로 설정하는 메소드입니다. |
| setOffsetTop | 부모 컴포넌트의 Top 위치를 기준으로 VideoPlayer 의 top 값을 픽셀단위로 설정하는 메소드입니다. |
| setOffsetWidth | VideoPlayer 의 너비를 픽셀단위로 설정하는 메소드입니다. |
| show | 스크립트로 동적 생성한 VideoPlayer 을(를) 화면에 표시하는 메소드입니다. |
| stop | VideoPlayer 에서 재생 중인 비디오를 중지시키고 재생위치를 처음으로 설정하는 메소드입니다. |

**Event**

| Name | Description |
| --- | --- |
| oncurrenttimechanged | VideoPlayer 에서 비디오 재생위치가 변경되거나 재생중일때 발생하는 이벤트입니다. |
| onerror | VideoPlayer 에서 비디오 파일에 대한 처리가 실패했을 때 발생하는 이벤트입니다. |
| onmove | VideoPlayer 의 위치가 이동했을 때 발생하는 이벤트입니다. |
| onplaystatuschanged | VideoPlayer 의 재생상태가 변경되었을 때 발생하는 이벤트입니다. |
| onsize | VideoPlayer 의 크기가 변경됐을 때 발생하는 이벤트입니다. |


---

### 속성 (Properties)

### -nexa-border

> Components > Component > VideoPlayer > Property > -nexa-border

**Description**

VideoPlayer 의 테두리를 설정하는 속성입니다.

**Syntax**

```javascript
VideoPlayer.border[= strborder]
```

**Setting Syntax**

```javascript
strborder ::= <line-width> <line-style> <color> [,<line-width> <line-style> <color>]{3}

<line-width> ::= 'thin' | 'medium' | 'thick' | <nWidth> 'px'
<line-style> ::= 'none' | 'hidden' | 'dotted' | 'dashed' | 'solid' | 'double' | 'groove' | 'ridge' | 'inset' | 'outset'
<color> ::= <NamedColor> | <NumericColor>

1회 입력 : top/right/bottom/left 에 모두 적용됩니다.
2회 입력 : top/bottom, right/left 에 첫번째 값부터 각각 적용됩니다.
3회 입력 : top, right/left, bottom 에 첫번째 값부터 각각 적용됩니다.
4회 입력 : top, right, bottom, left 에 첫번째 값부터 각각 적용됩니다.
```
```javascript
* XCSS
-nexa-border : 1px solid #999999;
-nexa-border : 1px solid #999999, 1px solid #999999, 1px solid #999999, 1px solid #999999;

* Script ( normal property )
this.VideoPlayer00.border = "1px solid #999999";
this.VideoPlayer00.border = "1px solid #999999, 1px solid #999999, 1px solid #999999, 1px solid #999999";
```
- **`<line-width>`** — 테두리 선의 두께를 설정합니다.

"thin" 설정 시 1 pixel 두께로 테두리가 표시됩니다.
"medium" 설정 시 3 pixel 두께로 테두리가 표시됩니다.
"thick" 설정 시  5 pixel 두께로 테두리가 표시됩니다.

<nWidth> 설정 시 <nWidth> pixel 두께로 테두리가 표시됩니다.
- **`<nWidth>`** — 테두리 선의 두께를 pixel 단위로 설정합니다.
"px" 단위는 생략할 수 없습니다.
- **`<line-style>`** — 테두리 선의 스타일을 설정합니다.

"none" 설정 시 테두리가 표시되지 않습니다.
"hidden" 설정 시 테두리가 표시되지 않습니다.
"dotted" 설정 시 테두리가 점선으로 표시됩니다.
"dashed" 설정 시 테두리가 긴 점선으로 표시됩니다.
"solid" 설정 시 테두리가 실선으로 표시됩니다.
"double" 설정 시 테두리가 두줄로 표시됩니다.
"groove" 설정 시 테두리가 들어간 것처럼 보이게 입체적으로 표시됩니다.
"ridge" 설정 시 테두리가 올라온 것처럼 보이게 입체적으로 표시됩니다.
"inset" 설정 시 컴포넌트가 들어간 것처럼 보이게 테두리가 입체적으로 표시됩니다.
"outset" 설정 시 컴포넌트가 올라온 것처럼 보이게 테두리가 입체적으로 표시됩니다.
- **`<color>`** — 테두리 색상을 색상이름 또는 색상코드로 설정합니다.
- **`<NamedColor>`** — "red","blue" 와 같이 미리 정의된 색상이름을 문자열로 설정합니다.
- **`<NumericColor>`** — 표시할 색의 색상코드입니다.

"#" 기호로 시작하는 6자리 hexadecimal 값으로 색상코드를 설정할 수 있습니다. (ex-"#FF00FF")
red, green, blue 값을 인자로 하는 "rgb()" 메소드로 색상코드를 설정할 수 있습니다. (ex-"rgb(255,0,255)")
red, green, blue, alpha 값을 인자로 하는 "rgba()" 메소드로 색상코드를 설정할 수 있습니다. (ex-"rgba(255,0,255,0.3)")
hue, saturation, lightness 값을 인자로 하는 "hsl()" 메소드로 색상코드를 설정할 수 있습니다. (ex-"hsl(120,50%,50%)")
hue, saturation, lightness, alpha 값을 인자로 하는 "hsla()" 메소드로 색상코드를 설정할 수 있습니다. (ex-"hsla(120,50%,50%,0.8)")

**Remark**

- -nexa-border 속성값을 설정하지 않으면 undefined 가 설정되고, "medium none #999999" 로 동작합니다.

- <line-style> 이 "double" 일 때 두 줄을 다른 색으로 설정할 수 없습니다.
   <color> 에 설정된 색이 두 줄에 모두 적용됩니다.

- 스크립트로 접근 시 속성명은 "border"를 사용해야 합니다.


◆ NRE 제약

- 각 테두리의 색상이나 두께를 다르게 설정했을 경우 top > right > bottom > left 순으로 겹쳐서 표시됩니다.

- <line-style> 에 "double","groove","ridge","inset","outset" 값을 설정해도 적용되지 않습니다.

- <line-width> 에 "thin","medium","thick" 값을 설정해도 적용되지 않습니다.


◆ WRE 제약

- 각 테두리의 색상이나 두께를 다르게 설정했을 경우 웹브라우저에 따라 테두리가 겹쳐지는 순서가 다르게 처리될 수 있습니다.

- XCSS 를 사용하지 않고 rgba(), hsl(), hsla() 메소드를 속성에 직접 설정할 경우 브라우저 버전에 따라 적용되지 않을 수 있습니다.


---

### accessibilityaction

> Components > Component > VideoPlayer > Property > accessibilityaction

**Description**

VideoPlayer에 선택상자 이동 시 accessibilitydescreadtype 속성에 따라 스크린 리더의 음성 출력 대상으로 포함할 action 정보를 설정하는 속성입니다.

**Syntax**

```javascript
VideoPlayer.accessibilityaction[= strAction]
```

**Setting Syntax**

```javascript
this.VideoPlayer00.accessibilityaction = "Accessibility Action Message"; 
this.VideoPlayer00.accessibilityaction = "Select by direction key";
```
- **`strAction`** — accessibilitydescreadtype 속성값에 "action"이 포함된 경우 스크린 리더의 음성 출력 대상으로 포함할 값을 설정합니다.

**Remark**

- enableaccessibility 속성값이 true 인 경우만 적용되는 속성입니다.

- 컴포넌트에 선택상자 이동 시 사용자가 추가적인 정보를 얻기 위해 필요한 동작을 안내하는 용도로 사용합니다. 
  예를 들어 컴포넌트 특성에 따라 다음과 같이 속성값을 설정할 수 있습니다.
  
  - CheckBox 컴포넌트의 경우 아래와 같은 안내문을 accessibilityaction 속성값으로 설정할 수 있습니다.
  "선택 상태를 전환하려면 스페이스 바를 누릅니다."
  
  - WebBrowser, WebView 컴포넌트의 경우 스크린 리더 사용자를 위한 안내문을 accessibilityaction 속성값으로 설정할 수 있습니다.
  "웹 브라우저 컴포넌트 내 콘텐츠를 이용하기 위해서는 가상커서를 해제해 주세요."
  그리고 WebBrowser, WebView 컴포넌트를 벗어났을 때 필요한 안내문을 accessibilityleavemessage 속성값으로 설정합니다.
  "가상커서를 설정해 주세요"


---

### accessibilitydesclevel

> Components > Component > VideoPlayer > Property > accessibilitydesclevel

**Description**

VideoPlayer 에 선택상자 이동 시 하위 컴포넌트의 접근성 출력여부를 설정하는 속성입니다.

**Syntax**

```javascript
VideoPlayer.accessibilitydesclevel[= enumLevel]
```

**Setting Syntax**

```javascript
enumLevel ::= 'all' | 'self' | 'child' | 'none'
```
```javascript
this.VideoPlayer00.accessibilitydesclevel = "all";
```
- **`"all"`** — VideoPlayer 와 자식 컴포넌트에 대한 접근성을 모두 출력합니다.
- **`"self"`** — VideoPlayer 의 접근성만 출력하고 자식 컴포넌트의 접근성은 출력하지 않습니다.
- **`"child"`** — VideoPlayer 의 접근성은 출력하지 않고, 자식 컴포넌트의 접근성은 출력합니다.
- **`"none"`** — VideoPlayer 와 자식 컴포넌트에 대한 접근성을 모두 출력하지 않습니다.

**Remark**

- accessibilitydesclevel 속성값을 설정하지 않으면 "all" 로 적용됩니다.

- Button 과 같이 자식 컴포넌트가 없는 컴포넌트는 "all" 또는 "child" 로 설정 시 "self" 로 적용됩니다.

- Div 와 같은 컨테이너 컴포넌트는 "child" 설정 시 실제 컨텐츠의 접근성만을 출력합니다.

- accessibilitydesclevel 속성은 선택상자의 이동에 영향을 주지 않으므로 방향키로 선택상자가 VideoPlayer 에 이동되게 하지 않으려면 accessibilityenable 속성을 설정하여야 합니다.


---

### accessibilitydescription

> Components > Component > VideoPlayer > Property > accessibilitydescription

**Description**

VideoPlayer에 선택상자 이동 시 accessibilitydescreadtype 속성에 따라 스크린 리더의 음성 출력 대상으로 포함할 description 정보를 설정하는 속성입니다.

**Syntax**

```javascript
VideoPlayer.accessibilitydescription[= strDescription]
```

**Setting Syntax**

```javascript
this.VideoPlayer00.accessibilitydescription = "Option";
```
- **`strDescription`** — accessibilitydescreadtype 속성값에 "description"이 포함된 경우 스크린 리더의 음성 출력 대상으로 포함할 값을 설정합니다.

**Remark**

- enableaccessibility 속성값이 true 인 경우만 적용되는 속성입니다.


---

### accessibilityenable

> Components > Component > VideoPlayer > Property > accessibilityenable

**Description**

VideoPlayer 에 선택상자 이동 시 접근성 관련 속성값 출력여부를 설정하는 속성입니다.

**Syntax**

```javascript
VideoPlayer.accessibilityenable[= bEnable]
```

**Setting Syntax**

```javascript
bEnable ::= 'true' | 'false'
```
```javascript
this.VideoPlayer00.accessibilityenable = false;
```
- **`true`** — VideoPlayer 에 선택상자가 이동되면 접근성 관련 속성값을 출력합니다.

방향키 또는 제스처로 선택상자 이동 시 Static 과 같이 포커스를 갖지 않는 컴포넌트에도 선택상자가 이동됩니다.
- **`false`** — VideoPlayer 에 선택상자가 이동되어도 접근성 관련 속성값을 출력하지 않습니다.

데스크탑 환경에서 방향키로 선택상자가 VideoPlayer 에 이동되지 않습니다.
모바일 환경에서 제스처로 선택상자가 VideoPlayer 에 이동되지 않습니다.

**Remark**

- accessibilityenable 속성값을 설정하지 않으면 true 로 적용됩니다.

- enableaccessibility 속성값이 true 인 경우만 적용되는 속성입니다.

- 선택상자 이동 시 컴포넌트의 ARIA-LABEL + Status + Role 순서로 정보가 조합되어 접근성으로 출력됩니다.
   각 정보의 조합순서는 스크린 리더의 종류에 따라 달라질 수 있습니다.
   > ARIA-LABEL : Environment 의 accessibilitydescreadtype 속성에 설정된 속성값이 반영됩니다.
   > Status : VideoPlayer 의 현재 상태가 반영됩니다.
   > Role : accessibilityrole 속성값이 반영됩니다. accessibilityrole 속성값을 설정하지 않으면 VideoPlayer 의 고유 Role 이 반영됩니다.

- accessibilityenable 속성값이 false 일 때 데스크탑 환경에서 방향키 외의 방법으로 선택상자를 VideoPlayer 로 이동시키면 스크린 리딩 프로그램에서 임의의 값을 출력할 수 있습니다.


◆ 컨테이너 컴포넌트 선택상자 이동 제약

아래와 같은 환경에서 컨테이너 컴포넌트로 선택상자가 이동하지 않고 컨테이너 컴포넌트 내에 배치된 컴포넌트로 이동합니다.
- Android, iOS/iPadOS 운영체제에서 실행 시 (Div, PopupDiv, TabpageControl, View)
- Windows 운영체제에서 센스리더 가상커서 사용 시 (Div, PopupDiv, View)


---

### accessibilitylabel

> Components > Component > VideoPlayer > Property > accessibilitylabel

**Description**

VideoPlayer에 선택상자 이동 시 accessibilitydescreadtype 속성에 따라 스크린 리더의 음성 출력 대상으로 포함할 label 정보를 설정하는 속성입니다.

**Syntax**

```javascript
VideoPlayer.accessibilitylabel[= strLabel]
```

**Setting Syntax**

```javascript
this.VideoPlayer00.accessibilitylabel = "OK Button"; 
this.VideoPlayer00.accessibilitylabel = "[@static00]"; 
this.VideoPlayer00.accessibilitylabel = "[@static00][@static01]"; 
this.VideoPlayer00.accessibilitylabel = "[@static00][@static01] OK Button";
```
- **`strLabel`** — accessibilitydescreadtype 속성값에 "label"이 포함된 경우 스크린 리더의 음성 출력 대상으로 포함할 값을 설정합니다.
문자열로 설정하거나 부모가 같은 컴포넌트의 accessibilitylabel 속성값을 참조할 수 있습니다.
다른 컴포넌트 참조 시에는 "[@ComponentID]" 형식으로 컴포넌트의 id를 대괄호와 "@" 문자를 사용하여 설정합니다.
참조하는 참조값에 해당하는 컴포넌트를 찾을 수 없는 경우에는 참조값 자체가 문자열로 처리됩니다.

**Remark**

- enableaccessibility 속성값이 true인 경우만 적용되는 속성입니다.

- accessibilitydescreadtype 속성값에 "label"이 포함되었으나 accessibilitylabel 속성값을 설정하지 않으면 스크린 리더의 음성 출력 대상에 포함하지 않습니다.


---

### accessibilityrole

> Components > Component > VideoPlayer > Property > accessibilityrole

**Description**

VideoPlayer에 선택상자 이동 시 스크린 리더가 읽어주는 Role 정보를 설정하는 속성입니다.

**Syntax**

```javascript
VideoPlayer.accessibilityrole[= enumRole]
```

**Setting Syntax**

```javascript
enumRole ::= 'none' | 'alert' | 'application' | 'button' | 'calendar' | 'chart' | 'checkbox' | 'columnheader' | 'combobox' | 'datepicker' | 'edit' | 'fileupload' | 'form' | 'frame' | 'grid' | 'gridcell' | 'groupbox' | 'heading' | 'heading1' | 'heading2' | 'heading3' | 'heading4' | 'heading5' | 'heading6' | 'image' | 'link' | 'listbox' | 'menubar' | 'progressbar' | 'radio' | 'rowheader' | 'scrollbar' | 'spin' | 'static' | 'statusbar' | 'tab' | 'tabpage' | 'textarea' | 'titlebar' | 'toolbar' | 'tooltip' | 'treegrid' | 'treeitem' | 'webbrowser'
```
```javascript
this.VideoPlayer00.accessibilityrole = "none"; 
this.VideoPlayer00.accessibilityrole = "button";
```
- **`enumRole`** — - "none": 스크린 리더에서 Role 정보를 처리하지 않습니다.

- 속성값을 설정하지 않으면 컴포넌트/오브젝트에 따라 기본 accessibilityrole 값이 적용됩니다.
"application": Application, MainFrame
"button": Button, FileDownload
"calendar": Calendar
"checkbox": CheckBox
"combobox": ComboBox
"edit": Edit, MaskEdit
"frame": FrameSet, ChildFrame
"fileupload": FileUpload
"form": Form, Div
"groupbox": GroupBox
"image": ImageViewer, Sketch
"listbox": ListBox
"menubar": Menu
"progressbar": ProgressBar
"radio": Radio
"spin": Spin
"static": Static
"tab": Tab
"tabpage": TabPage
"textarea": TextArea
"webbrowser": WebBrower

- Grid 컴포넌트의 경우 하위 오브젝트까지 Role을 적용합니다.
"grid": Grid
"gridcell": Grid Cell
"columnheader": Grid Column Head
"rowheader": Grid Row Head
"treegrid": TreeGrid
"treeitem": TreeItem, TreeCell

- 기능에 따라 컴포넌트 또는 오브젝트의 하위 오브젝트 영역에 아래와 같은 Role이 적용됩니다.
"alert": Alert, Confirm, UserConfirm
"chart": Chart
"datepicker": DatePicker
"link": Link
"scrollbar": ScrollBar
"statusbar": StatusBar
"titlebar": TitleBar
"toolbar": ToolBar
"tooltip": Tooltip

- "heading" 또는 "heading1"부터 "heading6" 사이의 값으로 속성값을 설정하면 role, aria-level 값을 처리합니다.
예를 들어 속성값을 "heading3"으로 설정하면 Generate되는 코드 태그에 role="heading", aria-level="3" 형식으로 값을 처리합니다.
속성값을 "heading"으로 설정 시에는 "heading1"을 설정한 것과 같은 동작을 처리합니다.
accessibilityheadingnexthotkey, accessibilityheadingprevhotkey 속성값으로 설정한 단축키로 이동 시 사용할 수 있습니다.

**Remark**

- enableaccessibility 속성값이 true 인 경우만 적용되는 속성입니다.

- accessibilityenable 속성값을 설정하지 않으면 VideoPlayer의 기본 Role 로 적용됩니다.

- 추가적인 속성값이 필요한 Role 설정 시 VideoPlayer에 해당 속성이 없으면 스크린 리더에 따라 정상적으로 정보를 읽지 못할 수 있습니다.


---

### aspectratio

> Components > Component > VideoPlayer > Property > aspectratio

**Description**

VideoPlayer 에 로드된 비디오 파일의 화면비율값을 갖는 읽기전용 속성입니다.

**Syntax**

```javascript
VideoPlayer.aspectratio
```

**Setting Syntax**

```javascript
trace( this.VideoPlayer00.aspectratio );               // "16:9" or "4:3" ...
```
- **`aspectratio`** — "가로:세로" 형식의 화면비율값을 갖습니다.


---

### autoplay

> Components > Component > VideoPlayer > Property > autoplay

**Description**

VideoPlayer 에 파일을 로드 시 자동으로 재생시킬 지 여부를 설정하는 속성입니다.

**Syntax**

```javascript
VideoPlayer.autoplay[= bAutoplay]
```

**Setting Syntax**

```javascript
bAutoplay ::= 'true' | 'false'
```
```javascript
this.VideoPlayer00.autoplay = true;
```
- **`true`** — 파일이 로드되면 자동으로 재생시킵니다.
- **`false`** — 파일이 로드되어도 자동으로 재생시키지 않습니다.

**Remark**

- autoplay 속성값을 설정하지 않으면 false 로 적용됩니다.


◆ Android WRE 제약

- Android 의 Chrome 웹브라우저는 지원하지 않는 속성입니다.


---

### bottom

> Components > Component > VideoPlayer > Property > bottom

**Description**

VideoPlayer 을(를) 표시하기 위한 하단 좌표값을 설정하는 속성입니다.

**Syntax**

```javascript
VideoPlayer.bottom[= nBottom]
```

**Setting Syntax**

```javascript
nBottom ::= [<strCompID> ':'] <nVal> ['px' | '%' | 'em' | 'rem']
```
```javascript
this.VideoPlayer00.bottom = 10; 
this.VideoPlayer00.bottom = "10px"; 
this.VideoPlayer00.bottom = "10%"; 
this.VideoPlayer00.bottom = "Button00:10px"; 
this.VideoPlayer00.bottom = "Button00:10%";
```
- **`<strCompID>`** — VideoPlayer 의 좌표값을 상대적으로 적용하기 위해 기준이 되는 컴포넌트의 ID 를 설정합니다.

기준 컴포넌트는 VideoPlayer 와 부모(Parent)가 동일하여야 합니다.
또한, Form 로딩 시 VideoPlayer 보다 먼저 생성되어야 합니다.

값을 설정하지 않거나 잘못된 값을 설정하면 부모(Parent) 를 기준으로 좌표값이 적용됩니다.
- **`<nVal>`** — VideoPlayer 의 하단 좌표값을 절대 길이 단위("px"), 비율 단위("%"), 상대 길이 단위("em", "rem")의 숫자로 설정합니다.

* <strCompID> 값을 생략했을 때 :
- "px": 부모(Parent)의 bottom 속성값을 기준으로 VideoPlayer의 하단 위치 설정
- "%": 부모(Parent)의 height 속성값을 기준으로 VideoPlayer의 하단 위치 결정
- "em": 컴포넌트의 font-size 속성값을 기준으로 VideoPlayer의 하단 위치 결정
- "rem": MainFrame font-size 속성값을 기준으로 VideoPlayer의 하단 위치 결정

* <strCompID> 값을 설정했을 때 :
- "px":  <strCompID>의 top 속성값을 기준으로 VideoPlayer의 하단 위치 결정
- "%": <strCompID>의 height 속성값을 기준으로 VideoPlayer의 하단 위치 결정
<strCompID> 값 설정 시 "em", "rem" 단위는 지원하지 않습니다.

**Remark**

- 좌표값에 단위를 생략할 경우 pixel 값으로 적용됩니다.

- 좌표값에 기준 컴포넌트 또는 단위문자를 포함할 경우 문자열 형태로 설정하여야 합니다.

◆ 기준 컴포넌트 설정 시 유의할 점

- 속성값을 기준 컴포넌트(<strCompID>)를 참조하도록 설정한 경우 다음 사항을 유의해주세요.

스크립트로 속성값에 설정한 기준 컴포넌트 값(<strCompID>)을 변경하는 경우 유효한 컴포넌트 값이 아니면(잘못된 컴포넌트 id, 부모 또는 stepindex가 다른 컴포넌트) 기준 컴포넌트 값은 무시하고 부모를 기준으로 좌표값(<nVal>)을 적용합니다.
스크립트로 기준 컴포넌트의 크기나 위치 속성값을 변경되는 경우(속성값에 영향을 주는 메소드 실행으로 인한 변경 포함) 변경된 속성값이 기준 컴포넌트를 참조하는 컴포넌트에 바로 반영되지 않습니다.
예를 들어 Button01 컴포넌트의 left 속성값이 "Button00:10px"인 경우 스크립트로 Button00 컴포넌트의 right 속성값이 변경하더라도 Button01 컴포넌트의 위치는 변경되지 않습니다.
기준 컴포넌트의 크기나 위치 속성값이 변경된 것을 기준 컴포넌트를 참조하는 컴포넌트에 반영하려면 스크립트에서 기준 컴포넌트의 크기나 위치 속성값 변경 후 Form 오브젝트의 resetScroll 메소드를 실행해주어야 합니다.


---

### cssclass

> Components > Component > VideoPlayer > Property > cssclass

**Description**

VideoPlayer 의 Style 을 XCSS 파일에 정의된 class 로 설정하는 속성입니다.

**Syntax**

```javascript
VideoPlayer.cssclass[= strcssclass]
```

**Setting Syntax**

```javascript
strcssclass ::= <ClassName> [ , &ltClassName> ]*
```
```javascript
this.VideoPlayer00.cssclass = "TestClass"; 
this.VideoPlayer00.cssclass = "TestClass,TestClass2";
```
- **`<ClassName>`** — XCSS 에 정의된 classname 을 설정합니다.

**Remark**

- XCSS 에 정의된 ".classname" 형식에서 "." 를 제외하고 "classname" 만 설정합니다.

- Style 에 적용되는 방식은 XCSS Attribute 지정과 동일하게 XCSS 내의 순서대로 적용됩니다.

- Status 를 적용하고자 할 경우는 "classname" 에 Status 를 붙여서 사용합니다.

- ',' 를 사용하여 class 를 다중으로 지정하게 되면 class 가 중첩되어 적용됩니다.
   중첩된 class 의 내용에 중복이 있다면 XCSS 내의 순서상 마지막으로 기술된 것이 적용됩니다.


◆ web runtime environment 제약

- web runtime environment 는 cssclass 명의 첫문자가 숫자일 경우 속성값이 적용되지 않으므로 주의하여야 합니다.


---

### currenttime

> Components > Component > VideoPlayer > Property > currenttime

**Description**

VideoPlayer 에서 비디오의 재생위치를 설정하는 속성입니다.

**Syntax**

```javascript
VideoPlayer.currenttime[= nCurrenttime]
```

**Setting Syntax**

```javascript
this.VideoPlayer00.currenttime = 5000;
```
- **`nCurrenttime`** — 비디오의 재생위치를 밀리초(MilliSecond) 단위로 설정합니다.

**Remark**

- currenttime 속성값을 설정하면 oncurrenttimechanged 이벤트가 발생합니다.

- Web Runtime Environment는 비디오 재생 중 currenttime 속성값이 지속적으로 갱신됩니다.


◆ Nexacro Runtime Environment 제약

- 비디오 재생중 currenttime 속성값이 갱신되지 않습니다.

- currenttime 속성은 비디오의 재생위치를 스크립트로 변경시키고자 할 때 사용됩니다.


---

### duration

> Components > Component > VideoPlayer > Property > duration

**Description**

VideoPlayer 에 로드된 비디오 파일의 전체 재생시간을 갖는 읽기전용 속성입니다.

**Syntax**

```javascript
VideoPlayer.duration
```

**Setting Syntax**

- **`duration`** — 비디오의 전체 재생시간을 밀리초(MilliSecond) 단위로 갖습니다.


---

### enable

> Components > Component > VideoPlayer > Property > enable

**Description**

VideoPlayer 의 사용가능 여부를 설정하는 속성입니다.

**Syntax**

```javascript
VideoPlayer.enable[= benable]
```

**Setting Syntax**

```javascript
benable ::= 'true' | 'false'
```
```javascript
this.VideoPlayer00.enable = true; 
this.VideoPlayer00.enable = false;
```
- **`true`** — VideoPlayer 을(를) 사용할 수 있게 설정합니다.
- **`false`** — VideoPlayer 을(를) 사용할 수 없게 설정합니다.

하위 컨트롤이 있을 경우 하위 컨트롤에 "disabled" Status 가 적용됩니다.

**Remark**

- enable 속성값을 설정하지 않으면 true 로 적용됩니다.

- enable 속성값이 false 이면 컴포넌트가 화면에 표시되지만 포커스나 입력을 받을 수 없습니다.

- Div 와 같은 컨테이너 컴포넌트의 enable 속성값을 false 로 설정하면 자식 컴포넌트도 모두 Disable 됩니다.


---

### enableevent

> Components > Component > VideoPlayer > Property > enableevent

**Description**

VideoPlayer 에서 이벤트를 발생시킬지 여부를 설정하는 속성입니다.

**Syntax**

```javascript
VideoPlayer.enableevent[= bEvent]
```

**Setting Syntax**

```javascript
bEvent ::= 'true' | 'false'
```
```javascript
this.VideoPlayer00.enableevent = true;  

this.VideoPlayer00.enableevent = false;
```
- **`true`** — VideoPlayer 에서 이벤트가 발생하도록 설정합니다.
- **`false`** — VideoPlayer 에서 이벤트가 발생하지 않도록 설정합니다.

**Remark**

- enableevent 속성을 false 로 설정하면 이벤트 발생으로 인한 처리속도 지연과 화면 깜빡임을 방지할 수 있습니다.


---

### flexgrow

> Components > Component > VideoPlayer > Property > flexgrow

**Description**

Layout 컨테이너 내 Main Axis(type 속성값이 "horizontal"이면 수평축, type 속성값이 "vertical"이면 수직축) 여백을 설정한 비율만큼 나누어 컴포넌트의 크기를 재조정하는 속성입니다.

**Syntax**

```javascript
VideoPlayer.flexgrow[= fVal]
```

**Setting Syntax**

- **`fVal`** — 컴포넌트의 크기를 재조정할 비율을 설정합니다.

**Remark**

- flexgrow 속성값을 설정하지 않으면 undefined로 설정되고 0 값으로 동작합니다.
  속성값이 0이면 크기를 재조정하지 않습니다.
    
- Layout 컨테이너 내 배치된 컴포넌트 중 flexgrow 속성값이 0보다 큰 값으로 설정된 컴포넌트를 대상으로 Main Axis의 여백을 설정한 비율만큼 나누어 크기에 적용합니다.

- maxwidth, maxheight 속성값을 설정한 경우에는 해당 크기보다 크게 크기가 조정되지는 않습니다.


◆ fittocontents 속성과 같이 사용하는 경우

아래와 같은 경우 컴포넌트의 flexgrow 속성값이 무시되고 fittocontents 속성만 적용됩니다.

- Layout 오브젝트 또는 Panel 컴포넌트의 type 속성값이 "horizontal"이고
  컴포넌트의 fittocontents 속성값이 "width" 또는 "both"일 때
- Layout 오브젝트 또는 Panel 컴포넌트의 type 속성값이 "vertical"이고
  컴포넌트의 fittocontents 속성값이 "height" 또는 "both"일 때


---

### flexshrink

> Components > Component > VideoPlayer > Property > flexshrink

**Description**

Layout 컨테이너 내 Main Axis(type 속성값이 "horizontal"이면 수평축, type 속성값이 "vertical"이면 수직축)에 컴포넌트가 배치되는 영역이 컨테이너 영역을 초과하지 않도록 설정한 비율만큼 나누어 컴포넌트의 크기를 재조정하는 속성입니다.

**Syntax**

```javascript
VideoPlayer.flexshrink[= fVal]
```

**Setting Syntax**

- **`fVal`** — 컴포넌트의 크기를 재조정할 비율을 설정합니다.

**Remark**

- flexshrink 속성값을 설정하지 않으면 undefined로 설정되고 1 값으로 동작합니다.
  속성값이 0이면 크기를 재조정하지 않습니다.
    
- Layout 컨테이너 내 배치된 컴포넌트 중 flexshrink 속성값이 0보다 큰 값으로 설정된 컴포넌트를 대상으로 Main Axis의 초과 영역을 설정한 비율만큼 나누어 크기에 적용합니다.

- minwidth, minheight 속성값을 설정한 경우에는 해당 크기보다 작게 크기가 조정되지는 않습니다.


◆ fittocontents 속성과 같이 사용하는 경우

아래와 같은 경우 컴포넌트의 flexshrink 속성값이 무시되고 fittocontents 속성만 적용됩니다.

- Layout 오브젝트 또는 Panel 컴포넌트의 type 속성값이 "horizontal"이고
  컴포넌트의 fittocontents 속성값이 "width" 또는 "both"일 때
- Layout 오브젝트 또는 Panel 컴포넌트의 type 속성값이 "vertical"이고
  컴포넌트의 fittocontents 속성값이 "height" 또는 "both"일 때


---

### height

> Components > Component > VideoPlayer > Property > height

**Description**

VideoPlayer 을(를) 표시하기 위한 높이를 설정하는 속성입니다.

**Syntax**

```javascript
VideoPlayer.height[= nHeight]
```

**Setting Syntax**

```javascript
nHeight ::= [<strCompID> ':'] <nVal> ['px' | '%' | 'em' | 'rem']
```
```javascript
this.VideoPlayer00.height = 10; 
this.VideoPlayer00.height = "10px"; 
this.VideoPlayer00.height = 10%"; 
this.VideoPlayer00.height = Button00:10%";
```
- **`<strCompID>`** — VideoPlayer 의 높이를 상대적으로 적용하기 위해 기준이 되는 컴포넌트의 ID 를 설정합니다.

기준 컴포넌트는 VideoPlayer 와 부모(Parent)가 동일하여야 합니다.
또한, Form 로딩 시 VideoPlayer 보다 먼저 생성되어야 합니다.

값을 설정하지 않거나 잘못된 값을 설정하면 부모(Parent) 를 기준으로 높이가 적용됩니다.
- **`<nVal>`** — VideoPlayer 의 높이를 절대 길이 단위("px"), 비율 단위("%"), 상대 길이 단위("em", "rem")의 숫자로 설정합니다.


* <strCompID> 값을 생략했을 때 :
- "px": VideoPlayer의 높이 설정
- "%": 부모(Parent)의 height 속성값을 기준으로 VideoPlayer의 높이 결정
- "em": 컴포넌트의 font-size 속성값을 기준으로 VideoPlayer의 높이 결정
- "rem": MainFrame font-size 속성값을 기준으로 VideoPlayer의 높이 결정

* <strCompID> 값을 설정했을 때 :
- "px":  <strCompID> 값은 무시되고 설정값이 VideoPlayer의 높이 결정
- "%": <strCompID>의 height 속성값을 기준으로 VideoPlayer의 높이 결정
<strCompID> 값 설정 시 "em", "rem" 단위는 지원하지 않습니다.

**Remark**

- 높이값에 단위를 생략할 경우 pixel 값으로 적용됩니다.

- 높이값에 기준 컴포넌트 또는 단위문자를 포함할 경우 문자열 형태로 설정하여야 합니다.

◆ 기준 컴포넌트 설정 시 유의할 점

- 속성값을 기준 컴포넌트(<strCompID>)를 참조하도록 설정한 경우 다음 사항을 유의해주세요.

스크립트로 속성값에 설정한 기준 컴포넌트 값(<strCompID>)을 변경하는 경우 유효한 컴포넌트 값이 아니면(잘못된 컴포넌트 id, 부모 또는 stepindex가 다른 컴포넌트) 기준 컴포넌트 값은 무시하고 부모를 기준으로 좌표값(<nVal>)을 적용합니다.
스크립트로 기준 컴포넌트의 크기나 위치 속성값을 변경되는 경우(속성값에 영향을 주는 메소드 실행으로 인한 변경 포함) 변경된 속성값이 기준 컴포넌트를 참조하는 컴포넌트에 바로 반영되지 않습니다.
예를 들어 Button01 컴포넌트의 left 속성값이 "Button00:10px"인 경우 스크립트로 Button00 컴포넌트의 right 속성값이 변경하더라도 Button01 컴포넌트의 위치는 변경되지 않습니다.
기준 컴포넌트의 크기나 위치 속성값이 변경된 것을 기준 컴포넌트를 참조하는 컴포넌트에 반영하려면 스크립트에서 기준 컴포넌트의 크기나 위치 속성값 변경 후 Form 오브젝트의 resetScroll 메소드를 실행해주어야 합니다.


---

### id

> Components > Component > VideoPlayer > Property > id

**Description**

VideoPlayer의 고유 식별자를 설정하는 속성입니다.

**Syntax**

```javascript
VideoPlayer.id
```

**Setting Syntax**

- **`id`** — VideoPlayer를 구별할 수 있는 유일값을 갖습니다.

**Remark**

-  addChild나 insertChild 메소드를 사용하여 컴포넌트를 등록할때 parent 가 자식컴포넌트를 구별하기 위한 고유한 key값입니다.

- 속성명이나 메소드명과 같은 예약어를 id 속성값으로 설정할 경우 오류가 발생할 수 있습니다.

- 컴포넌트의 속성은 아니지만 편의를 위하여 넥사크로 스튜디오에서 설정할 수 있는 기능이 제공됩니다.

- name 속성은 사용자가 지정한 컴포넌트의 이름으로 논리적으로 중복이 가능합니다.

- 넥사크로 스튜디오에서 컴포넌트를 생성하거나 id 값을 설정하면 name 속성에 동일한 값이 설정됩니다.


---

### layoutorder

> Components > Component > VideoPlayer > Property > layoutorder

**Description**

Layout 컨테이너 내 Main Axis(type 속성값이 "horizontal"이면 수평축, type 속성값이 "vertical"이면 수직축)에 컴포넌트가 배치되는 순서를 설정합니다.

**Syntax**

```javascript
VideoPlayer.layoutorder[= nVal]
```

**Setting Syntax**

- **`nVal`** — 컴포넌트의 배치 순서를 설정합니다.

**Remark**

- layoutorder 속성값을 설정하지 않으면 0으로 설정됩니다.

- 속성값이 음수인 경우에는 0으로 설정됩니다.

- 같은 값으로 2개 이상의 컴포넌트의 속성값을 설정할 수 있습니다. 
  속성값이 같은 경우에는 생성 순서(Z-Order)에 따라 배치 순서가 달라집니다.


---

### left

> Components > Component > VideoPlayer > Property > left

**Description**

VideoPlayer 을(를) 표시하기 위한 좌측 좌표값을 설정하는 속성입니다.

**Syntax**

```javascript
VideoPlayer.left[= nLeft]
```

**Setting Syntax**

```javascript
nLeft ::= [<strCompID> ':'] <nVal> ['px' | '%' | 'em' | 'rem']
```
```javascript
this.VideoPlayer00.left = 10; 
this.VideoPlayer00.left = "10px"; 
this.VideoPlayer00.left = "10%"; 
this.VideoPlayer00.left = "Button00:10px"; 
this.VideoPlayer00.left = "Button00:10%";
```
- **`<strCompID>`** — VideoPlayer 의 좌표값을 상대적으로 적용하기 위해 기준이 되는 컴포넌트의 ID 를 설정합니다.

기준 컴포넌트는 VideoPlayer 와 부모(Parent)가 동일하여야 합니다.
또한, Form 로딩 시 VideoPlayer 보다 먼저 생성되어야 합니다.

값을 설정하지 않거나 잘못된 값을 설정하면 부모(Parent) 를 기준으로 좌표값이 적용됩니다.
- **`<nVal>`** — VideoPlayer 의 좌측 좌표값을 절대 길이 단위("px"), 비율 단위("%"), 상대 길이 단위("em", "rem")의 숫자로 설정합니다.

* <strCompID> 값을 생략했을 때 :
- "px": 부모(Parent)의 left 속성값을 기준으로 VideoPlayer의 좌측 위치 설정
- "%": 부모(Parent)의 width 속성값을 기준으로 VideoPlayer의 좌측 위치 결정
- "em": 컴포넌트의 font-size 속성값을 기준으로 VideoPlayer의 좌측 위치 결정
- "rem": MainFrame font-size 속성값을 기준으로 VideoPlayer의 좌측 위치 결정

* <strCompID> 값을 설정했을 때 :
- "px":  <strCompID>의 right 속성값을 기준으로 VideoPlayer의 좌측 위치 결정
- "%": <strCompID>의 width 속성값을 기준으로 VideoPlayer의 좌측 위치 결정
<strCompID> 값 설정 시 "em", "rem" 단위는 지원하지 않습니다.

**Remark**

- 좌표값에 단위를 생략할 경우 pixel 값으로 적용됩니다.

- 좌표값에 기준 컴포넌트 또는 단위문자를 포함할 경우 문자열 형태로 설정하여야 합니다.

◆ 기준 컴포넌트 설정 시 유의할 점

- 속성값을 기준 컴포넌트(<strCompID>)를 참조하도록 설정한 경우 다음 사항을 유의해주세요.

스크립트로 속성값에 설정한 기준 컴포넌트 값(<strCompID>)을 변경하는 경우 유효한 컴포넌트 값이 아니면(잘못된 컴포넌트 id, 부모 또는 stepindex가 다른 컴포넌트) 기준 컴포넌트 값은 무시하고 부모를 기준으로 좌표값(<nVal>)을 적용합니다.
스크립트로 기준 컴포넌트의 크기나 위치 속성값을 변경되는 경우(속성값에 영향을 주는 메소드 실행으로 인한 변경 포함) 변경된 속성값이 기준 컴포넌트를 참조하는 컴포넌트에 바로 반영되지 않습니다.
예를 들어 Button01 컴포넌트의 left 속성값이 "Button00:10px"인 경우 스크립트로 Button00 컴포넌트의 right 속성값이 변경하더라도 Button01 컴포넌트의 위치는 변경되지 않습니다.
기준 컴포넌트의 크기나 위치 속성값이 변경된 것을 기준 컴포넌트를 참조하는 컴포넌트에 반영하려면 스크립트에서 기준 컴포넌트의 크기나 위치 속성값 변경 후 Form 오브젝트의 resetScroll 메소드를 실행해주어야 합니다.


---

### maxheight

> Components > Component > VideoPlayer > Property > maxheight

**Description**

VideoPlayer 이(가) 화면에 표시되는 최대 높이를 설정하는 속성입니다.

**Syntax**

```javascript
VideoPlayer.maxheight[= nMaxHeight]
```

**Setting Syntax**

```javascript
nMaxHeight ::= <nVal> ['px']
```
```javascript
this.VideoPlayer00.maxheight = 20; 
this.VideoPlayer00.maxheight = "20px";
```
- **`<nVal>`** — VideoPlayer 이(가) 화면에 표시되는 높이의 최대값을 pixel 단위의 숫자로 설정합니다.

**Remark**

- height 속성값이 maxheight 보다 크면 화면에 표시되는 VideoPlayer 의 높이는 maxheight 속성값으로 적용됩니다.
  표시되는 높이만 적용되고 height 속성값은 변경되지 않습니다.

- maxheight 속성값에 단위문자를 포함할 경우 문자열 형태로 설정하여야 합니다.

- maxheight 속성값을 minheight 속성값보다 작게 설정하면 minheight 속성값이 maxheight 속성값으로 설정됩니다.

- Position 관련 속성(left, top 등)에 기준컴포넌트를 설정하여 VideoPlayer 의 높이가 상대적으로 설정될 경우에도 적용되는 속성입니다.
   maxheight > height > top(상대값) > bottom(상대값) > top(절대값) > bottom(절대값) 순서로 설정값이 적용됩니다.
   이 때, height, top, bottom 값 중 먼저 2개가 설정되면 나머지는 무시됩니다.

- fittocontents 속성이 있는 컴포넌트는 속성값에 의해 높이가 자동 설정될 때도 적용되는 속성입니다.


---

### maxwidth

> Components > Component > VideoPlayer > Property > maxwidth

**Description**

VideoPlayer 이(가) 화면에 표시되는 최대 너비를 설정하는 속성입니다.

**Syntax**

```javascript
VideoPlayer.maxwidth[= nMaxWidth]
```

**Setting Syntax**

```javascript
nMaxWidth ::= <nVal> ['px']
```
```javascript
this.VideoPlayer00.maxwidth = 20; 
this.VideoPlayer00.maxwidth = "20px";
```
- **`<nVal>`** — VideoPlayer 이(가) 화면에 표시되는 너비의 최대값을 pixel 단위의 숫자로 설정합니다.

**Remark**

- width 속성값이 maxwidth 보다 크면 화면에 표시되는 VideoPlayer 의 너비는 maxwidth 속성값으로 적용됩니다.
  표시되는 너비만 적용되고 width 속성값은 변경되지 않습니다.

- maxwidth 속성값에 단위문자를 포함할 경우 문자열 형태로 설정하여야 합니다.

- maxwidth 속성값을 minwidth 속성값보다 작게 설정하면 minwidth 속성값이 maxwidth 속성값으로 설정됩니다.

- Position 관련 속성(left, top 등)에 기준컴포넌트를 설정하여 VideoPlayer 의 너비가 상대적으로 설정될 경우에도 적용되는 속성입니다.
   maxwidth > width > left(상대값) > right(상대값) > left(절대값) > right(절대값) 순서로 설정값이 적용됩니다.
   이 때, width, left, right 값 중 먼저 2개가 설정되면 나머지는 무시됩니다.

- fittocontents 속성이 있는 컴포넌트는 속성값에 의해 너비가 자동 설정될 때도 적용되는 속성입니다.


---

### minheight

> Components > Component > VideoPlayer > Property > minheight

**Description**

VideoPlayer 이(가) 화면에 표시되는 최소 높이를 설정하는 속성입니다.

**Syntax**

```javascript
VideoPlayer.minheight[= nMinHeight]
```

**Setting Syntax**

```javascript
nMinHeight ::= <nVal> ['px']
```
```javascript
this.VideoPlayer00.minheight = 20; 
this.VideoPlayer00.minheight = "20px";
```
- **`<nVal>`** — VideoPlayer 이(가) 화면에 표시되는 높이의 최소값을 pixel 단위의 숫자로 설정합니다.

**Remark**

- height 속성값이 minheight 보다 작다면 화면에 표시되는 VideoPlayer 의 높이는 minheight 속성값으로 적용됩니다.
  표시되는 높이만 적용되고 height 속성값은 변경되지 않습니다.

- minheight 속성값에 단위문자를 포함할 경우 문자열 형태로 설정하여야 합니다.

- minheight 속성값을 maxheight 속성값보다 크게 설정하면 maxheight 속성값이 minheight 속성값으로 설정됩니다.

- Position 관련 속성(left, top 등)에 기준컴포넌트를 설정하여 VideoPlayer 의 높이가 상대적으로 설정될 경우에도 적용되는 속성입니다.
   minheight > height > top(상대값) > bottom(상대값) > top(절대값) > bottom(절대값) 순서로 설정값이 적용됩니다.
   이 때, height, top, bottom 값 중 먼저 2개가 설정되면 나머지는 무시됩니다.

- fittocontents 속성이 있는 컴포넌트는 속성값에 의해 높이가 자동 설정될 때도 적용되는 속성입니다.


---

### minwidth

> Components > Component > VideoPlayer > Property > minwidth

**Description**

VideoPlayer 이(가) 화면에 표시되는 최소 너비를 설정하는 속성입니다.

**Syntax**

```javascript
VideoPlayer.minwidth[= nMinWidth]
```

**Setting Syntax**

```javascript
nMinWidth ::= <nVal> ['px']
```
```javascript
this.VideoPlayer00.minwidth = 20; 
this.VideoPlayer00.minwidth = "20px";
```
- **`<nVal>`** — VideoPlayer 이(가) 화면에 표시되는 너비의 최소값을 pixel 단위의 숫자로 설정합니다.

**Remark**

- width 속성값이 minwidth 보다 작으면 화면에 표시되는 VideoPlayer 의 너비는 minwidth 속성값으로 적용됩니다.
  표시되는 너비만 적용되고 width 속성값은 변경되지 않습니다.

- minwidth 속성값에 단위문자를 포함할 경우 문자열 형태로 설정하여야 합니다.

- minwidth 속성값을 maxwidth 속성값보다 크게 설정하면 maxwidth 속성값이 minwidth 속성값으로 설정됩니다.

- Position 관련 속성(left, top 등)에 기준컴포넌트를 설정하여 VideoPlayer 의 너비가 상대적으로 설정될 경우에도 적용되는 속성입니다.
   minwidth > width > left(상대값) > right(상대값) > left(절대값) > right(절대값) 순서로 설정값이 적용됩니다.
   이 때, width, left, right 값 중 먼저 2개가 설정되면 나머지는 무시됩니다.

- fittocontents 속성이 있는 컴포넌트는 속성값에 의해 너비가 자동 설정될 때도 적용되는 속성입니다.


---

### mute

> Components > Component > VideoPlayer > Property > mute

**Description**

VideoPlayer 에서 비디오 재생 시 음소거 여부를 설정하는 속성입니다.

**Syntax**

```javascript
VideoPlayer.mute[= bMute]
```

**Setting Syntax**

```javascript
bMute ::= 'true' | 'false'
```
- **`"true"`** — 비디오 재생 시 음소거 되어 소리를 재생하지 않습니다.
- **`"false"`** — 비디오 재생 시 소리를 재생합니다.

**Remark**

- mute 속성값을 설정하지 않으면 "false" 로 적용됩니다.

- 기본 UI(ControlBar)에서 음소거를 설정하여도 mute 속성값은 변경되지 않습니다.


---

### name

> Components > Component > VideoPlayer > Property > name

**Description**

VideoPlayer 의 이름을 설정하는 속성입니다.

**Syntax**

```javascript
VideoPlayer.name[= strName]
```

**Setting Syntax**

- **`strName`** — 컴포넌트의 이름을 설정합니다.

**Remark**

- name 속성은 사용자가 지정한 컴포넌트의 이름으로 논리적으로 중복이 가능합니다.

- 넥사크로 스튜디오에서 컴포넌트를 생성하거나 id 값을 설정하면 name 속성에 동일한 값이 설정됩니다.


---

### parent

> Components > Component > VideoPlayer > Property > parent

**Description**

VideoPlayer 의 부모 오브젝트를 갖는 읽기전용 속성입니다.

**Syntax**

```javascript
VideoPlayer.parent
```

**Setting Syntax**

```javascript
var objParent = this.VideoPlayer00.parent;
```

**Remark**

- parent 속성은 일반적으로 컴포넌트가 속한 Form을 갖습니다.

- VideoPlayer 이(가) Div 와 같은 컨테이너 컴포넌트에 자식으로 존재한다면 parent 속성은 해당 컨테이너 컴포넌트를 갖습니다.

- Calendar 와 같이 서브컨트롤이 존재하는 컴포넌트에서 서브컨트롤의 parent 속성은 해당 컴포넌트를 갖습니다.


---

### positionstep

> Components > Component > VideoPlayer > Property > positionstep

**Description**

부모 오브젝트가 화면분할기능을 사용할 때 VideoPlayer 이(가) 표시될 화면의 인덱스를 설정하는 속성입니다.

**Syntax**

```javascript
VideoPlayer.positionstep[= nStep]
```

**Setting Syntax**

```javascript
this.VideoPlayer00.positionstep = 0; 
this.VideoPlayer00.positionstep = -1;
```
- **`nStep`** — VideoPlayer 이(가) 표시될 분할화면의 인덱스를 설정합니다.
"-1" 로 설정하면 모든 분할화면에 VideoPlayer 이(가) 표시됩니다.

분할화면의 인덱스는 "0" 부터 시작합니다.

**Remark**

- positionstep 에 설정된 인덱스값과 일치하는 분할화면에만 컴포넌트가 표시됩니다.

- 분할화면에 표시되지 않는 컴포넌트의 visible 속성값은 "false"로 변경되지 않습니다.


---

### repeat

> Components > Component > VideoPlayer > Property > repeat

**Description**

VideoPlayer 에서 비디오를 반복재생할 지 여부를 설정하는 속성입니다.

**Syntax**

```javascript
VideoPlayer.repeat[= bRepeat]
```

**Setting Syntax**

```javascript
bRepeat ::= 'true' | 'false'
```
```javascript
this.VideoPlayer00.repeat = true;
```
- **`"true"`** — 비디오를 반복재생합니다.
- **`"false"`** — 비디오를 반복재생하지 않습니다.

**Remark**

- repeat 속성값을 설정하지 않으면 "false" 로 적용됩니다.

- 컨트롤바에서 일시정지 버튼을 누르거나 pause(), stop() 메소드가 호출될 때까지 반복재생합니다.


---

### right

> Components > Component > VideoPlayer > Property > right

**Description**

VideoPlayer 를 표시하기 위한 우측 좌표값을 설정하는 속성입니다.

**Syntax**

```javascript
VideoPlayer.right[= nRight]
```

**Setting Syntax**

```javascript
nRight ::= [<strCompID> ':'] <nVal> ['px' | '%' | 'em' | 'rem']
```
```javascript
this.VideoPlayer00.right = 10; 
this.VideoPlayer00.right = "10px"; 
this.VideoPlayer00.right = "10%"; 
this.VideoPlayer00.right = "Button00:10px"; 
this.VideoPlayer00.right = "Button00:10%";
```
- **`<strCompID>`** — VideoPlayer 의 좌표값을 상대적으로 적용하기 위해 기준이 되는 컴포넌트의 ID 를 설정합니다.

기준 컴포넌트는 VideoPlayer 와 부모(Parent)가 동일하여야 합니다.
또한, Form 로딩 시 VideoPlayer 보다 먼저 생성되어야 합니다.

값을 설정하지 않거나 잘못된 값을 설정하면 부모(Parent) 를 기준으로 좌표값이 적용됩니다.
- **`<nVal>`** — VideoPlayer 의 우측 좌표값을 절대 길이 단위("px"), 비율 단위("%"), 상대 길이 단위("em", "rem")의 숫자로 설정합니다.

* <strCompID> 값을 생략했을 때 :
- "px": 부모(Parent)의 right 속성값을 기준으로 VideoPlayer의 우측 위치 설정
- "%": 부모(Parent)의 width 속성값을 기준으로 VideoPlayer의 우측 위치 결정
- "em": 컴포넌트의 font-size 속성값을 기준으로 VideoPlayer의 우측 위치 결정
- "rem": MainFrame font-size 속성값을 기준으로 VideoPlayer의 우측 위치 결정

* <strCompID> 값을 설정했을 때 :
- "px":  <strCompID>의 left 속성값을 기준으로 VideoPlayer의 우측 위치 결정
- "%": <strCompID>의 width 속성값을 기준으로 VideoPlayer의 우측 위치 결정
<strCompID> 값 설정 시 "em", "rem" 단위는 지원하지 않습니다.

**Remark**

- 좌표값에 단위를 생략할 경우 pixel 값으로 적용됩니다.

- 좌표값에 기준 컴포넌트 또는 단위문자를 포함할 경우 문자열 형태로 설정하여야 합니다.

◆ 기준 컴포넌트 설정 시 유의할 점

- 속성값을 기준 컴포넌트(<strCompID>)를 참조하도록 설정한 경우 다음 사항을 유의해주세요.

스크립트로 속성값에 설정한 기준 컴포넌트 값(<strCompID>)을 변경하는 경우 유효한 컴포넌트 값이 아니면(잘못된 컴포넌트 id, 부모 또는 stepindex가 다른 컴포넌트) 기준 컴포넌트 값은 무시하고 부모를 기준으로 좌표값(<nVal>)을 적용합니다.
스크립트로 기준 컴포넌트의 크기나 위치 속성값을 변경되는 경우(속성값에 영향을 주는 메소드 실행으로 인한 변경 포함) 변경된 속성값이 기준 컴포넌트를 참조하는 컴포넌트에 바로 반영되지 않습니다.
예를 들어 Button01 컴포넌트의 left 속성값이 "Button00:10px"인 경우 스크립트로 Button00 컴포넌트의 right 속성값이 변경하더라도 Button01 컴포넌트의 위치는 변경되지 않습니다.
기준 컴포넌트의 크기나 위치 속성값이 변경된 것을 기준 컴포넌트를 참조하는 컴포넌트에 반영하려면 스크립트에서 기준 컴포넌트의 크기나 위치 속성값 변경 후 Form 오브젝트의 resetScroll 메소드를 실행해주어야 합니다.


---

### showcontrolbar

> Components > Component > VideoPlayer > Property > showcontrolbar

**Description**

VideoPlayer 에 기본 UI(ControlBar)를 표시할 지 여부를 설정하는 속성입니다.

**Syntax**

```javascript
VideoPlayer.showcontrolbar[= bShow]
```

**Setting Syntax**

```javascript
bShow ::= 'true' | 'false'
```
```javascript
this.VideoPlayer00.showcontrolbar = true;
```
- **`"true"`** — 기본 UI(ControlBar)를 표시합니다.
- **`"false"`** — 기본 UI(ControlBar)를 표시하지 않습니다.

**Remark**

- showcontrollbar 속성값을 설정하지 않으면 "false" 로 적용됩니다.

- 기본 UI(ControlBar)에서는 최대화, 최소화 기능을 지원하지 않습니다.

- 기본 UI(ControlBar)에서 음량을 변경하여도 volume, mute 속성값은 변경되지 않습니다.

- 일부 환경에서 지원되지 않는 속성입니다.


---

### tablecellarea

> Components > Component > VideoPlayer > Property > tablecellarea

**Description**

Layout 컨테이너 내 가상의 Table Cell 영역 내에서 컴포넌트가 배치되는 영역을 설정합니다.

**Syntax**

```javascript
VideoPlayer.tablecellarea[= strArea]
```

**Setting Syntax**

```javascript
strArea ::= <rowstart> <rowend> '/' <columnstart> <columnend> | <strArea>

<strPosition> ::= <rowstart> <rowend> '/' <columnstart> <columnend>
```
- **`<strPosition>`** — 컴포넌트를 배치할 Cell 좌표를 설정합니다.

rowstart, rowend 또는 columnstart, columnend 구분은 공백문자를 사용합니다.
rowstart, rowend 또는 columnstart, columnend 가 같은(하나의 Cell 영역만 설정) 경우에는 하나의 값만 표기할 수 있습니다.
예를 들어 0 / 0 또는 0 0 / 0 0 으로 설정할 수 있습니다.
- **`<strArea>`** — 컴포넌트를 배치할 tabletemplatearea 속성에서 설정한 Cell 이름을 설정합니다.

**Remark**

- 유효하지 않은 값을 설정한 경우에는 layoutorder, tabledirection 속성값에 따라 배치됩니다.

- 같은 영역에 2개 이상의 컴포넌트를 배치할 수 있습니다.

- row와 column의 start, end는 반대 방향으로 설정할 수 있습니다.
  예를 들어 "2 0 / 2 0"과 "0 2 / 0 2"은 같은 영역으로 처리합니다.


---

### taborder

> Components > Component > VideoPlayer > Property > taborder

**Description**

탭키 입력으로 컴포넌트간 포커스를 이동할 때 VideoPlayer 의 순서를 설정하는 속성입니다.

**Syntax**

```javascript
VideoPlayer.taborder[= nTabOrder]
```

**Setting Syntax**

- **`nTabOrder`** — 0 이상의 정수로 설정합니다.

**Remark**

- taborder 속성값이 작은 컴포넌트에 포커스가 먼저 이동되고, 속성값이 클수록 나중에 포커스가 이동됩니다.

- taborder 속성값이 동일한 컴포넌트는 폼에 먼저 생성된 컴포넌트에 포커스가 먼저 이동됩니다.

- 스크립트로 taborder 속성값 변경 시 포커스 이동 순서가 변경됩니다.

- Div 와 같은 컨테이너 컴포넌트는 자식 컴포넌트 레벨에서 포커스 이동 순서가 별도로 정의됩니다.


---

### tabstop

> Components > Component > VideoPlayer > Property > tabstop

**Description**

탭키 입력으로 컴포넌트간 포커스를 이동할 때 VideoPlayer 이(가) 포커스를 받을 지 여부를 설정하는 속성입니다.

**Syntax**

```javascript
VideoPlayer.tabstop[= bTabStop]
```

**Setting Syntax**

```javascript
bTabStop ::= 'true' | 'false'
```
- **`"true"`** — VideoPlayer 이(가) 탭키 입력에 의한 포커스를 받습니다.
- **`"false"`** — VideoPlayer 이(가) 탭키 입력에 의한 포커스를 받지 않습니다.

**Remark**

- tabstop 속성값이 "false"인 경우 taborder 속성값과 관계없이 포커스가 해당 컴포넌트를 건너뜁니다.

- tabstop 속성값에 관계없이 마우스로 직접 포커스를 주거나, setfocus() 메소드 사용 시 포커스를 갖습니다.

- Div 와 같은 컨테이너 컴포넌트에서 포커스를 받을 수 있는 자식 컴포넌트가 존재하면 해당 컴포넌트가 포커스를 갖습니다.


---

### top

> Components > Component > VideoPlayer > Property > top

**Description**

VideoPlayer 을(를) 표시하기 위한 상단 좌표값을 설정하는 속성입니다.

**Syntax**

```javascript
VideoPlayer.top[= nTop]
```

**Setting Syntax**

```javascript
nTop ::= [<strCompID> ':'] <nVal> ['px' | '%' | 'em' | 'rem']
```
```javascript
this.VideoPlayer00.top = 10; 
this.VideoPlayer00.top = "10px"; 
this.VideoPlayer00.top = "10%"; 
this.VideoPlayer00.top = "Button00:10px"; 
this.VideoPlayer00.top = "Button00:10%";
```
- **`<strCompID>`** — VideoPlayer 의 좌표값을 상대적으로 적용하기 위해 기준이 되는 컴포넌트의 ID 를 설정합니다.

기준 컴포넌트는 VideoPlayer 와 부모(Parent)가 동일하여야 합니다.
또한, Form 로딩 시 VideoPlayer 보다 먼저 생성되어야 합니다.

값을 설정하지 않거나 잘못된 값을 설정하면 부모(Parent) 를 기준으로 좌표값이 적용됩니다.
- **`<nVal>`** — VideoPlayer의 상단 좌표값을 절대 길이 단위("px"), 비율 단위("%"), 상대 길이 단위("em", "rem")의 숫자로 설정합니다.

* <strCompID> 값을 생략했을 때 :
- "px": 부모(Parent)의 top 속성값을 기준으로 VideoPlayer의 상단 위치 설정
- "%": 부모(Parent)의 height 속성값을 기준으로 VideoPlayer의 상단 위치 결정
- "em": 컴포넌트의 font-size 속성값을 기준으로 VideoPlayer의 상단 위치 결정
- "rem": MainFrame font-size 속성값을 기준으로 VideoPlayer의 상단 위치 결정

* <strCompID> 값을 설정했을 때 :
- "px":  <strCompID>의 bottom 속성값을 기준으로 VideoPlayer의 상단 위치 결정
- "%": <strCompID>의 height 속성값을 기준으로 VideoPlayer의 상단 위치 결정
<strCompID> 값 설정 시 "em", "rem" 단위는 지원하지 않습니다.

**Remark**

- 좌표값에 단위를 생략할 경우 pixel 값으로 적용됩니다.

- 좌표값에 기준 컴포넌트 또는 단위문자를 포함할 경우 문자열 형태로 설정하여야 합니다.

◆ 기준 컴포넌트 설정 시 유의할 점

- 속성값을 기준 컴포넌트(<strCompID>)를 참조하도록 설정한 경우 다음 사항을 유의해주세요.

스크립트로 속성값에 설정한 기준 컴포넌트 값(<strCompID>)을 변경하는 경우 유효한 컴포넌트 값이 아니면(잘못된 컴포넌트 id, 부모 또는 stepindex가 다른 컴포넌트) 기준 컴포넌트 값은 무시하고 부모를 기준으로 좌표값(<nVal>)을 적용합니다.
스크립트로 기준 컴포넌트의 크기나 위치 속성값을 변경되는 경우(속성값에 영향을 주는 메소드 실행으로 인한 변경 포함) 변경된 속성값이 기준 컴포넌트를 참조하는 컴포넌트에 바로 반영되지 않습니다.
예를 들어 Button01 컴포넌트의 left 속성값이 "Button00:10px"인 경우 스크립트로 Button00 컴포넌트의 right 속성값이 변경하더라도 Button01 컴포넌트의 위치는 변경되지 않습니다.
기준 컴포넌트의 크기나 위치 속성값이 변경된 것을 기준 컴포넌트를 참조하는 컴포넌트에 반영하려면 스크립트에서 기준 컴포넌트의 크기나 위치 속성값 변경 후 Form 오브젝트의 resetScroll 메소드를 실행해주어야 합니다.


---

### url

> Components > Component > VideoPlayer > Property > url

**Description**

VideoPlayer 에 재생시킬 비디오 파일을 설정하는 속성입니다.

**Syntax**

```javascript
VideoPlayer.url[= strUrl]
```

**Setting Syntax**

```javascript
strUrl ::= <absolute-url> | <relative-url>
```
```javascript
this.VideoPlayer00.url = "http://localhost:8080/Video/test.avi";     // 웹경로  

this.VideoPlayer00.url = "../Video/test.avi";                                // 상대경로 
this.VideoPlayer00.url = "Video::test.avi";                                   // TypeDefinition의 Services 경로
```
- **`<absolute-url>`** — 웹에 위치한 파일을 "http://경로/파일명", "https://경로/파일명" 형식으로 설정합니다.

웹에 접근 가능한 환경이어야 합니다.
- **`<relative-url>`** — TypeDefinition 영역의 Services 에 정의된 Prefix 를 사용하여 파일의 위치를 설정합니다.

현재 Form의 위치를 기준으로 한 상대경로를 사용하여 설정합니다.

**Remark**

- Windows NRE 에서 지원하는 포맷은 "https://support.microsoft.com/en-us/kb/245608" 사이트를 참조하세요.

- Android NRE 에서 지원하는 포맷은 "https://developer.android.com/guide/appendix/media-formats.html" 사이트를 참조하세요.

- iOS/iPadOS NRE, Chrome 에서 지원하는 포맷은 "https://developer.mozilla.org/en-US/docs/Web/HTML/Supported_media_formats" 사이트를 참조하세요.


---

### videosize

> Components > Component > VideoPlayer > Property > videosize

**Description**

VideoPlayer 에 로드된 비디오 파일의 실제 너비와 높이를 배열형태로 갖는 읽기전용 속성입니다.

**Syntax**

```javascript
VideoPlayer.videosize
```

**Setting Syntax**

```javascript
var nWidth = this.VideoPlayer00.videosize[0] ;
var nHeight = this.VideoPlayer00.videosize[1] ;
```
- **`videosize`** — 비디오 파일의 너비를 배열의 첫번째 원소값으로 갖습니다.
비디오 파일의 높이를 배열의 두번째 원소값으로 갖습니다.


---

### visible

> Components > Component > VideoPlayer > Property > visible

**Description**

VideoPlayer 이(가) 화면에 표시될지 여부를 설정하는 속성입니다.

**Syntax**

```javascript
VideoPlayer.visible[= bVisible]
```

**Setting Syntax**

```javascript
bVisible ::= 'true' | 'false'
```
```javascript
this.VideoPlayer00.visible = true; 
this.VideoPlayer00.visible = false;
```
- **`"true"`** — VideoPlayer 을(를) 화면에 표시합니다.
- **`"false"`** — VideoPlayer 을(를) 화면에 표시하지 않습니다.

**Remark**

- visible 속성값을 변경하는 즉시 컴포넌트의 표시 여부가 화면에 반영됩니다.

- visible 속성값이 "false"이면 컴포넌트가 화면에 표시되지 않고, 포커스를 받을 수 없으므로 입력이나 사용이 불가능합니다.

- Div 와 같은 컨테이너 컴포넌트의 visible 속성값을 "false"로 설정하면 자식 컴포넌트도 함께 화면에 표시되지 않습니다.

- visible 속성값이 "false"이어도 컴포넌트는 존재하기 때문에 스크립트로 제어가 가능합니다.

- 넥사크로 스튜디오에서는 visible 속성값을 'false'로 설정해도 디자인화면에서 사라지지 않습니다.


---

### volume

> Components > Component > VideoPlayer > Property > volume

**Description**

VideoPlayer 의 음량을 설정하는 속성입니다.

**Syntax**

```javascript
VideoPlayer.volume[= nVolume]
```

**Setting Syntax**

```javascript
this.VideoPlayer00.volume = 60;
```
- **`nVolume`** — 음량을 0 ~ 100 사이의 숫자값으로 설정합니다.

음수로 설정 시 0 으로 적용됩니다.
100 초과값을 설정 시 100 으로 적용됩니다.

**Remark**

- 기본 UI(ControlBar)에서 음량을 변경하여도 volume 속성값은 변경되지 않습니다.


◆ iOS/iPadOS 제약

- iOS/iPadOS 환경에서는 시스템 음량을 기준으로 동작하며 음량값을 수정할 수 없습니다.


---

### width

> Components > Component > VideoPlayer > Property > width

**Description**

VideoPlayer 을(를) 표시하기 위한 너비를 설정하는 속성입니다.

**Syntax**

```javascript
VideoPlayer.width[= nWidth]
```

**Setting Syntax**

```javascript
nWidth ::= [<strCompID> ':'] <nVal> ['px' | '%' | 'em' | 'rem']
```
```javascript
this.VideoPlayer00.width = 10; this.VideoPlayer00.width = "10px"; 
this.VideoPlayer00.width = "10%"; 
this.VideoPlayer00.width = "Button00:10%";
```
- **`<strCompID>`** — VideoPlayer 의 너비를 상대적으로 적용하기 위해 기준이 되는 컴포넌트의 ID 를 설정합니다.

기준 컴포넌트는 VideoPlayer 와 부모(Parent)가 동일하여야 합니다.
또한, Form 로딩 시 VideoPlayer 보다 먼저 생성되어야 합니다.

값을 설정하지 않거나 잘못된 값을 설정하면 부모(Parent) 를 기준으로 너비가 적용됩니다.
- **`<nVal>`** — VideoPlayer 의 너비를 절대 길이 단위("px"), 비율 단위("%"), 상대 길이 단위("em", "rem")의 숫자로 설정합니다.

* <strCompID> 값을 생략했을 때 :
- "px": VideoPlayer의 너비 설정
- "%": 부모(Parent)의 width 속성값을 기준으로 VideoPlayer의 너비 결정
- "em": 컴포넌트의 font-size 속성값을 기준으로 VideoPlayer의 너비 결정
- "rem": MainFrame font-size 속성값을 기준으로 VideoPlayer의 너비 결정

* <strCompID> 값을 설정했을 때 :
- "px":  <strCompID> 값은 무시되고 설정값이 VideoPlayer의 너비 결정
- "%": <strCompID>의 width 속성값을 기준으로 VideoPlayer의 너비 결정
<strCompID> 값 설정 시 "em", "rem" 단위는 지원하지 않습니다.

**Remark**

- 너비값에 단위를 생략할 경우 pixel 값으로 적용됩니다.

- 너비값에 기준 컴포넌트 또는 단위문자를 포함할 경우 문자열 형태로 설정하여야 합니다.

◆ 기준 컴포넌트 설정 시 유의할 점

- 속성값을 기준 컴포넌트(<strCompID>)를 참조하도록 설정한 경우 다음 사항을 유의해주세요.

스크립트로 속성값에 설정한 기준 컴포넌트 값(<strCompID>)을 변경하는 경우 유효한 컴포넌트 값이 아니면(잘못된 컴포넌트 id, 부모 또는 stepindex가 다른 컴포넌트) 기준 컴포넌트 값은 무시하고 부모를 기준으로 좌표값(<nVal>)을 적용합니다.
스크립트로 기준 컴포넌트의 크기나 위치 속성값을 변경되는 경우(속성값에 영향을 주는 메소드 실행으로 인한 변경 포함) 변경된 속성값이 기준 컴포넌트를 참조하는 컴포넌트에 바로 반영되지 않습니다.
예를 들어 Button01 컴포넌트의 left 속성값이 "Button00:10px"인 경우 스크립트로 Button00 컴포넌트의 right 속성값이 변경하더라도 Button01 컴포넌트의 위치는 변경되지 않습니다.
기준 컴포넌트의 크기나 위치 속성값이 변경된 것을 기준 컴포넌트를 참조하는 컴포넌트에 반영하려면 스크립트에서 기준 컴포넌트의 크기나 위치 속성값 변경 후 Form 오브젝트의 resetScroll 메소드를 실행해주어야 합니다.


---

### 메서드 (Methods)

### addEvent

> Components > Component > VideoPlayer > Method > addEvent

**Description**

VideoPlayer 에 특정 이벤트를 선언하여 추가하는 메소드입니다.

**Syntax**

```javascript
VideoPlayer.addEvent( strEventID )
```

**Parameters**

```
VideoPlayer 에 추가할 이벤트의 ID 를 문자열로 설정합니다.
```

**Return**

이벤트 추가에 성공하면 true 를 반환합니다.

이벤트 추가에 실패하거나 선언되어 있는 이벤트 ID 설정 시 false 를 반환합니다.

**Remark**

- Composite Component 와 같은 사용자 컴포넌트에 특정 이벤트를 추가할 때 사용하는 메소드입니다.

- VideoPlayer 에서 특정 이벤트를 제거하려면 removeEvent() 메소드를 사용하여야 합니다.


---

### addEventHandler

> Components > Component > VideoPlayer > Method > addEventHandler

**Description**

VideoPlayer 의 특정 이벤트에 핸들러 함수를 추가하는 메소드입니다.

**Syntax**

```javascript
VideoPlayer.addEventHandler( strEventID, objFunc, objTarget )
```

**Parameters**

```
핸들러 함수가 추가될 이벤트의 ID를 설정합니다.
```

**Return**

이벤트에 추가된 핸들러 함수의 인덱스를 반환합니다.
동일한 핸들러 함수가 이미 있다면 해당 핸들러 함수의 인덱스를 반환합니다.
정상적으로 추가되지 않은 경우에는 -1 을 반환합니다.


---

### addEventHandlerLookup

> Components > Component > VideoPlayer > Method > addEventHandlerLookup

**Description**

함수를 검색하여 VideoPlayer 의 특정 이벤트에 핸들러 함수로 추가하는 메소드입니다.

**Syntax**

```javascript
VideoPlayer.addEventHandlerLookup( strEventID, strFunc, objTarget )
```

**Parameters**

```
핸들러 함수가 추가될 이벤트의 ID를 설정합니다.
```

**Return**

이벤트에 추가된 핸들러 함수의 인덱스를 반환합니다.
동일한 핸들러 함수가 이미 있다면 해당 핸들러 함수의 인덱스를 반환합니다.
정상적으로 추가되지 않은 경우에는 "-1"을 반환합니다.

**Remark**

- strFunc 이름의 함수를 objTarget 영역부터 상위로 올라가며 검색합니다.
  검색된 함수가 있다면 이벤트에 핸들러 함수로 추가합니다.


---

### clearEventHandler

> Components > Component > VideoPlayer > Method > clearEventHandler

**Description**

특정 이벤트에 등록된 핸들러 함수를 모두 제거하는 메소드입니다.

**Syntax**

```javascript
VideoPlayer.clearEventHandler( strEventID )
```

**Parameters**

```
모든 핸들러 함수를 제거할 이벤트의 ID를 설정합니다.
```

**Return**

특정 이벤트에서 제거된 핸들러 함수의 갯수를 반환합니다.

**Remark**

- 특정 핸들러 함수만 제거하려면 removeEventHandler() 메소드를 사용하여야 합니다.


---

### destroy

> Components > Component > VideoPlayer > Method > destroy

**Description**

스크립트에서 동적으로 생성한 VideoPlayer 을(를) 삭제하는 메소드입니다.

**Syntax**

```javascript
VideoPlayer.destroy()
```

**Parameters**

var bSucc = this.VideoPlayer00.destroy();

**Return**

VideoPlayer 이(가) 정상적으로 삭제되면 true 를 반환합니다.

VideoPlayer 이(가) 정상적으로 삭제되지 않으면 false 를 반환합니다.

**Remark**

- 동적으로 생성한 VideoPlayer 을(를) 완전히 삭제하기 위해서는 destroy() 메소드를 호출해야 합니다.

**See Also**

Form.addChildForm.insertChildForm.removeChildVideoPlayer.initVideoPlayer.show


---

### findEventHandler

> Components > Component > VideoPlayer > Method > findEventHandler

**Description**

VideoPlayer 의 특정 이벤트에서 핸들러 함수를 찾아 인덱스를 반환하는 메소드입니다.

**Syntax**

```javascript
VideoPlayer.findEventHandler( strEventID, objFunc, objTarget )
```

**Parameters**

```
핸들러 함수를 찾을 이벤트의 ID를 설정합니다.
```

**Return**

특정 이벤트에서 찾은 핸들러 함수의 인덱스를 반환합니다.

특정 이벤트에 찾으려는 핸들러 함수가 존재하지 않으면 -1 을 반환합니다.

**Remark**

- 핸들러 함수의 인덱스는 0 부터 시작합니다.


---

### getEventHandler

> Components > Component > VideoPlayer > Method > getEventHandler

**Description**

VideoPlayer 의 특정 이벤트에서 지정된 인덱스에 해당하는 핸들러 함수를 반환하는 메소드입니다.

**Syntax**

```javascript
VideoPlayer.getEventHandler( strEventID, nIdx )
```

**Parameters**

```
핸들러 함수를 얻을 이벤트의 ID를 설정합니다.
```

**Return**

지정된 인덱스의 핸들러 함수 오브젝트를 반환합니다.

지정된 인덱스에 핸들러 함수가 존재하지 않는다면 null 을 반환합니다.


---

### getOffsetBottom

> Components > Component > VideoPlayer > Method > getOffsetBottom

**Description**

부모 컴포넌트의 Top 위치를 기준으로 VideoPlayer 의 bottom 값을 픽셀단위의 숫자로 반환하는 메소드입니다.

**Syntax**

```javascript
VideoPlayer.getOffsetBottom();
```

**Parameters**

var nBottom = this.VideoPlayer.getOffsetBottom();

**Return**

부모 컴포넌트의 Top 위치를 기준으로 VideoPlayer 의 bottom 값을 픽셀단위의 숫자로 반환합니다.

**Remark**

- 화면에 표시된 부모 컴포넌트의 Top 위치를 기준으로 VideoPlayer 의 bottom 값을 동적으로 계산합니다.


---

### getOffsetHeight

> Components > Component > VideoPlayer > Method > getOffsetHeight

**Description**

VideoPlayer 의 높이를 픽셀단위의 숫자로 반환하는 메소드입니다.

**Syntax**

```javascript
VideoPlayer.getOffsetHeight();
```

**Parameters**

var nHeight = this.VideoPlayer.getOffsetHeight();

**Return**

VideoPlayer 의 높이를 픽셀단위의 숫자로 변환하여 반환합니다.

**Remark**

- 화면에 표시된 VideoPlayer 의 높이를 동적으로 계산합니다.


---

### getOffsetLeft

> Components > Component > VideoPlayer > Method > getOffsetLeft

**Description**

부모 컴포넌트의 Left 위치를 기준으로 VideoPlayer 의 left 값을 픽셀단위의 숫자로 반환하는 메소드입니다.

**Syntax**

```javascript
VideoPlayer.getOffsetLeft();
```

**Parameters**

var nleft = this.VideoPlayer.getOffsetLeft();

**Return**

부모 컴포넌트의 Left 위치를 기준으로 VideoPlayer 의 left 값을 픽셀단위의 숫자로 반환합니다.

**Remark**

- 화면에 표시된 부모 컴포넌트의 Left 위치를 기준으로 VideoPlayer 의 left 값을 동적으로 계산합니다.


---

### getOffsetRight

> Components > Component > VideoPlayer > Method > getOffsetRight

**Description**

부모 컴포넌트의 Left 위치를 기준으로 VideoPlayer 의 right 값을 픽셀단위의 숫자로 반환하는 메소드입니다.

**Syntax**

```javascript
VideoPlayer.getOffsetRight();
```

**Parameters**

var nRight = this.VideoPlayer.getOffsetRight();

**Return**

부모 컴포넌트의 Left 위치를 기준으로 VideoPlayer 의 right 값을 픽셀단위의 숫자로 반환합니다.

**Remark**

- 화면에 표시된 부모 컴포넌트의 Left 위치를 기준으로 VideoPlayer 의 right 값을 동적으로 계산합니다.


---

### getOffsetTop

> Components > Component > VideoPlayer > Method > getOffsetTop

**Description**

부모 컴포넌트의 Top 위치를 기준으로 VideoPlayer 의 top 값을 픽셀단위의 숫자로 반환하는 메소드입니다.

**Syntax**

```javascript
VideoPlayer.getOffsetTop();
```

**Parameters**

var nTop = this.VideoPlayer.getOffsetTop();

**Return**

부모 컴포넌트의 Top 위치를 기준으로 VideoPlayer 의 top 값을 픽셀단위의 숫자로 반환합니다.

**Remark**

- 화면에 표시된 부모 컴포넌트의 Top 위치를 기준으로 VideoPlayer 의 top 값을 동적으로 계산합니다.


---

### getOffsetWidth

> Components > Component > VideoPlayer > Method > getOffsetWidth

**Description**

VideoPlayer 의 너비를 픽셀단위의 숫자로 반환하는 메소드입니다.

**Syntax**

```javascript
VideoPlayer.getOffsetWidth();
```

**Parameters**

var nWidth = this.VideoPlayer.getOffsetWidth();

**Return**

VideoPlayer 의 너비를 픽셀단위의 숫자로 변환하여 반환합니다.

**Remark**

- 화면에 표시된 VideoPlayer 의 너비를 동적으로 계산합니다.


---

### getPixelBottom

> Components > Component > VideoPlayer > Method > getPixelBottom

**Description**

VideoPlayer 의 bottom 속성값을 픽셀단위로 반환하는 메소드입니다.

**Syntax**

```javascript
VideoPlayer.getPixelBottom();
```

**Parameters**

var nbottom = this.VideoPlayer.getPixelBottom();

**Return**

VideoPlayer 의 bottom 속성값을 픽셀단위로 반환합니다.

bottom 속성값을 설정하지 않았을 경우 null 을 반환합니다.

**Remark**

- bottom 속성에 설정한 값의 단위가 퍼센트(%)여도 픽셀(pixel) 단위의 값을 반환합니다.


---

### getPixelHeight

> Components > Component > VideoPlayer > Method > getPixelHeight

**Description**

VideoPlayer 의 height 속성값을 픽셀단위로 반환하는 메소드입니다.

**Syntax**

```javascript
VideoPlayer.getPixelHeight();
```

**Parameters**

var nheight = this.VideoPlayer.getPixelHeight();

**Return**

VideoPlayer 의 height 속성값을 픽셀단위로 반환합니다.

height 속성값을 설정하지 않았을 경우 null 을 반환합니다.

**Remark**

- height 속성에 설정한 값의 단위가 퍼센트(%)여도 픽셀(pixel) 단위의 값을 반환합니다.


---

### getPixelLeft

> Components > Component > VideoPlayer > Method > getPixelLeft

**Description**

VideoPlayer 의 left 속성값을 픽셀단위로 반환하는 메소드입니다.

**Syntax**

```javascript
VideoPlayer.getPixelLeft();
```

**Parameters**

var nleft = this.VideoPlayer.getPixelLeft();

**Return**

VideoPlayer 의 left 속성값을 픽셀단위로 반환합니다.

left 속성값을 설정하지 않았을 경우 null 을 반환합니다.

**Remark**

- left 속성에 설정한 값의 단위가 퍼센트(%)여도 픽셀(pixel) 단위의 값을 반환합니다.


---

### getPixelRight

> Components > Component > VideoPlayer > Method > getPixelRight

**Description**

VideoPlayer 의 right 속성값을 픽셀단위로 반환하는 메소드입니다.

**Syntax**

```javascript
VideoPlayer.getPixelRight();
```

**Parameters**

var nright = this.VideoPlayer.getPixelRight();

**Return**

VideoPlayer 의 right 속성값을 픽셀단위로 반환합니다.

right 속성값을 설정하지 않았을 경우 null 을 반환합니다.

**Remark**

- right 속성에 설정한 값의 단위가 퍼센트(%)여도 픽셀(pixel) 단위의 값을 반환합니다.


---

### getPixelTop

> Components > Component > VideoPlayer > Method > getPixelTop

**Description**

VideoPlayer 의 top 속성값을 픽셀단위로 반환하는 메소드입니다.

**Syntax**

```javascript
VideoPlayer.getPixelTop();
```

**Parameters**

var ntop = this.VideoPlayer.getPixelTop();

**Return**

VideoPlayer 의 top 속성값을 픽셀단위로 반환합니다.

top 속성값을 설정하지 않았을 경우 null 을 반환합니다.

**Remark**

- top 속성에 설정한 값의 단위가 퍼센트(%)여도 픽셀(pixel) 단위의 값을 반환합니다.


---

### getPixelWidth

> Components > Component > VideoPlayer > Method > getPixelWidth

**Description**

VideoPlayer 의 width 속성값을 픽셀단위로 반환하는 메소드입니다.

**Syntax**

```javascript
VideoPlayer.getPixelWidth();
```

**Parameters**

var nwidth = this.VideoPlayer.getPixelWidth();

**Return**

VideoPlayer 의 width 속성값을 픽셀단위로 반환합니다.

width 속성값을 설정하지 않았을 경우 null 을 반환합니다.

**Remark**

- width 속성에 설정한 값의 단위가 퍼센트(%)여도 픽셀(pixel) 단위의 값을 반환합니다.


---

### init

> Components > Component > VideoPlayer > Method > init

**Description**

스크립트로 VideoPlayer 을(를) 동적 생성한 후에 초기화하는 메소드입니다.

**Syntax**

```javascript
VideoPlayer.init( strName, vLeft, vTop , vWidth, vHeight [, vRight, vBottom, [vMinWidth, [vMaxWidth, [vMinHeight, [vMaxHeight]]]]] )
```

**Parameters**

```
VideoPlayer 의 ID를 문자열로 설정합니다.
```

**Return**

없음

**Remark**

- 인수값에 기준 컴포넌트를 포함하면 VideoPlayer 의 위치는 기준 컴포넌트를 기준으로 상대적으로 결정됩니다.
  vLeft, vTop, vWidth, vHeight, vRight, vBottom 값에 기준 컴포넌트를 포함할 때는 "ID:Value" 형태로 설정하여야 합니다.

- 기준 컴포넌트는 VideoPlayer 와(과) 부모(Parent)가 동일하여야 하며, Form 로딩 시 VideoPlayer 보다 먼저 생성되어야 합니다.
  기준 컴포넌트를 설정하지 않거나 잘못된 값을 설정하면 부모(Parent)를 기준으로 좌표값이 적용됩니다.

- 인수값에 기준 컴포넌트 또는 단위를 포함할 경우 문자열로 설정하여야 합니다.

- 스크립트에서 동적으로 컴포넌트를 생성한 후에만 사용할 수 있습니다.
  동적으로 생성한 컴포넌트가 아닌 경우에는 오동작이 발생할수 있습니다.

- show() 메소드로 컴포넌트가 화면에 표시되면 초기화가 끝난 상태입니다.
  초기화가 끝난 후에 init() 메소드를 호출하면 오류가 발생하며 설정한 값은 적용되지 않습니다.

- 동적으로 생성한 컴포넌트를 컨테이너 컴포넌트에 추가하기 위해서는 addChild() 메소드나 insertChild() 메소드를 사용하여야 합니다.

**See Also**

Form.addChildForm.insertChildForm.removeChildVideoPlayer.destroyVideoPlayer.show


---

### insertEventHandler

> Components > Component > VideoPlayer > Method > insertEventHandler

**Description**

VideoPlayer 의 특정 이벤트에 핸들러 함수를 지정한 위치에 삽입하는 메소드입니다.

**Syntax**

```javascript
VideoPlayer.insertEventHandler( strEventID, nIndex, objFunc, objTarget )
```

**Parameters**

```
핸들러 함수가 삽입될 이벤트의 ID 를 설정합니다.
```

**Return**

이벤트에 삽입된 핸들러 함수의 인덱스를 반환합니다.
동일한 핸들러 함수가 이미 있다면 해당 핸들러 함수의 인덱스를 반환합니다.

핸들러 함수가 정상적으로 삽입되지 않은 경우에는 -1 을 반환합니다.

**Remark**

- 첫번째 핸들러 함수의 인덱스는 0 입니다.


---

### move

> Components > Component > VideoPlayer > Method > move

**Description**

VideoPlayer 의 위치와 크기를 변경하는 메소드입니다.

**Syntax**

```javascript
VideoPlayer.move( vLeft, vTop [, vWidth, vHeight [, vRight, vBottom]] )
```

**Parameters**

```
VideoPlayer 의 left 속성값을 pixel 또는 비율("%") 단위의 숫자로 설정합니다.

* 값에 기준 컴포넌트를 포함하여 설정했을 때 :
pixel 단위로 설정 시 기준 컴포넌트의 right 속성값을 기준으로 VideoPlayer 의 left 가 결정됩니다.
비율("%") 단위로 설정 시 기준 컴포넌트의 width 속성값을 기준으로 VideoPlayer 의 left 가 결정됩니다.
```

**Return**

없음

**Remark**

- 인수값에 기준 컴포넌트를 포함하면 VideoPlayer 의 위치는 기준 컴포넌트를 기준으로 상대적으로 결정됩니다.
  vLeft, vTop, vWidth, vHeight, vRight, vBottom 값에 기준 컴포넌트를 포함할 때는 "ID:Value" 형태로 설정하여야 합니다.

- 기준 컴포넌트는 VideoPlayer 와(과) 부모(Parent)가 동일하여야 하며, Form 로딩 시 VideoPlayer 보다 먼저 생성되어야 합니다.
  기준 컴포넌트를 설정하지 않거나 잘못된 값을 설정하면 부모(Parent)를 기준으로 좌표값이 적용됩니다.

- 인수값에 기준 컴포넌트 또는 단위를 포함할 경우 문자열로 설정하여야 합니다.


---

### pause

> Components > Component > VideoPlayer > Method > pause

**Description**

VideoPlayer 에서 재생 중인 비디오를 일시정지하는 메소드입니다.

**Syntax**

```javascript
VideoPlayer.pause();
```

**Return**

없음

**Remark**

- 메소드 수행 시 onplaystatuschanged 이벤트가 발생합니다.


---

### play

> Components > Component > VideoPlayer > Method > play

**Description**

VideoPlayer 에 로드된 비디오 파일을 재생하는 메소드입니다.

**Syntax**

```javascript
VideoPlayer.play()
```

**Parameters**

this.VideoPlayer00.play();

**Return**

없음

**Remark**

- 메소드 수행 시 onplaystatuschanged 이벤트가 발생합니다.

- 일시정지 상태일 경우 정지된 위치부터 재생이 시작됩니다.

- web runtime environment 는 비디오가 재생되는 동안 oncurrenttimechanged 이벤트가 지속적으로 발생합니다.


---

### removeEvent

> Components > Component > VideoPlayer > Method > removeEvent

**Description**

VideoPlayer 에 선언된 특정 이벤트를 삭제하는 메소드입니다.

**Syntax**

```javascript
VideoPlayer.removeEvent( strEventID )
```

**Parameters**

```
VideoPlayer 에서 삭제할 이벤트의 ID 를 문자열로 설정합니다.
```

**Return**

이벤트 삭제에 성공하면 true 를 반환합니다.

이벤트 삭제에 실패하거나 선언되지 않은 이벤트 ID 설정 시 false 를 반환합니다.

**Remark**

- Composite Component 와 같은 사용자 컴포넌트에서 특정 이벤트를 삭제할 때 사용하는 메소드입니다.

- 이벤트에 핸들러 함수가 정의되어 있을 경우 이벤트를 삭제할 수 없습니다.

- VideoPlayer 에 특정 이벤트를 추가하려면 addEvent() 메소드를 사용하여야 합니다.

- VideoPlayer 에 기본으로 선언되어 있는 이벤트 삭제 시 오동작 할 수 있으므로 주의하여야 합니다.


---

### removeEventHandler

> Components > Component > VideoPlayer > Method > removeEventHandler

**Description**

VideoPlayer 의 특정 이벤트에서 핸들러 함수를 제거하는 메소드입니다.

**Syntax**

```javascript
VideoPlayer.removeEventHandler( strEventID, objFunc, objTarget )
```

**Parameters**

```
핸들러 함수를 제거할 이벤트의 ID를 설정합니다.
```

**Return**

핸들러 함수 제거에 성공하면 1 을 반환합니다.

핸들러 함수 제거에 실패하면 0 을 반환합니다.

**Remark**

- 핸들러 함수가 제거되면 핸들러 함수의 인덱스가 0 번부터 재조정됩니다.

- 이벤트에 등록된 핸들러 함수를 모두 제거하려면 clearEventHandler() 메소드를 사용하여야 합니다.


---

### removeEventHandlerLookup

> Components > Component > VideoPlayer > Method > removeEventHandlerLookup

**Description**

VideoPlayer 의 특정 이벤트에서 검색된 함수와 동일한 핸들러 함수를 제거하는 메소드입니다.

**Syntax**

```javascript
VideoPlayer.removeEventHandlerLookup( strEventID, strFunc, objTarget )
```

**Parameters**

```
핸들러 함수를 제거할 이벤트의 ID를 설정합니다.
```

**Return**

핸들러 함수 제거에 성공하면 1 을 반환합니다.

핸들러 함수 제거에 실패하면 0 을 반환합니다.

**Remark**

- 핸들러 함수가 제거되면 핸들러 함수의 인덱스가 0 번부터 재조정됩니다.

- 이벤트에 등록된 핸들러 함수를 모두 제거하려면 clearEventHandler() 메소드를 사용하여야 합니다.


---

### resize

> Components > Component > VideoPlayer > Method > resize

**Description**

VideoPlayer 의 크기를 변경하는 메소드입니다.

**Syntax**

```javascript
VideoPlayer.resize( vWidth, vHeight )
```

**Parameters**

```
VideoPlayer 의 width 속성값을 pixel 또는 비율("%") 단위의 숫자로 설정합니다.
음수값을 설정할 수 없습니다.

* 값에 기준 컴포넌트를 포함하여 설정했을 때 :
pixel 단위로 설정 시 기준 컴포넌트값은 무시되고 pixel 값으로 VideoPlayer 의 width 가 결정됩니다.
비율("%") 단위로 설정 시 기준 컴포넌트의 width 속성값을 기준으로 VideoPlayer 의 width 가 결정됩니다.
```

**Return**

없음

**Remark**

- VideoPlayer 의 left 속성값이 null 이 아니면 resize() 메소드 실행 후 VideoPlayer 의 우측 위치가 변경됩니다.
  또한, 메소드 실행 후 right 속성값은 null 이 됩니다.

- VideoPlayer 의 left 속성값이 null 이면 resize() 메소드 실행 후 VideoPlayer 의 좌측 위치가 변경됩니다.
  또한, 메소드 실행 후 left 속성값은 null 로 유지됩니다.

- VideoPlayer 의 top 속성값이 null 이 아니면 resize() 메소드 실행 후 VideoPlayer 의 하단 위치가 변경됩니다.
  또한, 메소드 실행 후 bottom 속성값은 null 이 됩니다.

- VideoPlayer 의 top 속성값이 null 이면 resize() 메소드 실행 후 VideoPlayer 의 상단 위치가 변경됩니다.
  또한, 메소드 실행 후 top 속성값은 null 로 유지됩니다.

- 인수값에 기준 컴포넌트를 포함하면 VideoPlayer 의 위치는 기준 컴포넌트를 기준으로 상대적으로 결정됩니다.
  vWidth, vHeight 값에 기준 컴포넌트를 포함할 때는 "ID:Value" 형태로 설정하여야 합니다.

- 기준 컴포넌트는 VideoPlayer 와(과) 부모(Parent)가 동일하여야 하며, Form 로딩 시 VideoPlayer 보다 먼저 생성되어야 합니다.
  기준 컴포넌트를 설정하지 않거나 잘못된 값을 설정하면 부모(Parent)를 기준으로 좌표값이 적용됩니다.

- 인수값에 기준 컴포넌트 또는 단위를 포함할 경우 문자열로 설정하여야 합니다.


---

### rewind

> Components > Component > VideoPlayer > Method > rewind

**Description**

VideoPlayer 에 로드된 비디오의 재생위치를 처음으로 설정하는 메소드입니다.

**Syntax**

```javascript
VideoPlayer.rewind();
```

**Return**

없음

**Remark**

- Windows NRE 만 사용할 수 있는 메소드입니다.

- 메소드 수행 시 onplaystatuschanged 이벤트가 발생합니다.


---

### setEventHandler

> Components > Component > VideoPlayer > Method > setEventHandler

**Description**

VideoPlayer 의 특정 이벤트에서 첫번째 핸들러 함수를 변경하는 메소드입니다.

**Syntax**

```javascript
VideoPlayer.setEventHandler( strEventID, objFunc, objTarget )
```

**Parameters**

```
핸들러 함수를 변경할 이벤트의 ID를 설정합니다.
```

**Return**

첫번째 핸들러 함수 변경에 성공하면 0 을 반환합니다.

첫번째 핸들러 함수 변경에 실패하면 -1 을 반환합니다.

**Remark**

- 인덱스가 0 번인 핸들러 함수를 새로운 함수로 대체합니다.

- 인덱스가 0 번인 핸들러 함수가 없다면 0 번 인덱스에 새로운 함수를 핸들러 함수로 추가합니다.


---

### setEventHandlerLookup

> Components > Component > VideoPlayer > Method > setEventHandlerLookup

**Description**

VideoPlayer 의 특정 이벤트에서 첫번째 핸들러 함수를 검색된 함수로 변경하는 메소드입니다.

**Syntax**

```javascript
VideoPlayer.setEventHandlerLookup( strEventID, strFunc, objTarget )
```

**Parameters**

```
핸들러 함수를 변경할 이벤트의 ID를 설정합니다.
```

**Return**

첫번째 핸들러 함수 변경에 성공하면 0 을 반환합니다.

첫번째 핸들러 함수 변경에 실패하면 -1 을 반환합니다.

**Remark**

- 인덱스가 0 번인 핸들러 함수를 검색된 함수로 대체합니다.

- 인덱스가 0 번인 핸들러 함수가 없다면 0 번 인덱스에 검색된 함수를 핸들러 함수로 추가합니다.


---

### setOffsetBottom

> Components > Component > VideoPlayer > Method > setOffsetBottom

**Description**

부모 컴포넌트의 Top 위치를 기준으로 VideoPlayer 의 bottom 값을 픽셀단위로 설정하는 메소드입니다.

**Syntax**

```javascript
VideoPlayer.setOffsetBottom( nBottom );
```

**Parameters**

```
부모 컴포넌트의 Top 위치를 기준으로 VideoPlayer 의 bottom 값을 픽셀단위의 숫자로 설정합니다.
```

**Return**

없음

**Remark**

- 메소드 실행 전 VideoPlayer 에 설정된 초기 속성에 따라 메소드 실행 후 설정되는 속성이 달라집니다.
   > VideoPlayer 에 top, height 속성이 설정되어 있었다면 메소드 실행 후 top, height 속성이 설정됩니다.
   > VideoPlayer 에 top, bottom 속성이 설정되어 있었다면 메소드 실행 후 top, height 속성이 설정됩니다.
   > VideoPlayer 에 height, bottom 속성이 설정되어 있었다면 메소드 실행 후 height, bottom 속성이 설정됩니다.


---

### setOffsetHeight

> Components > Component > VideoPlayer > Method > setOffsetHeight

**Description**

VideoPlayer 의 높이를 픽셀단위로 설정하는 메소드입니다.

**Syntax**

```javascript
VideoPlayer.setOffsetHeight( nHeight );
```

**Parameters**

```
VideoPlayer 의 높이를 픽셀단위의 숫자로 설정합니다.
```

**Return**

없음

**Remark**

- 메소드 실행 전 VideoPlayer 에 설정된 초기 속성에 따라 메소드 실행 후 설정되는 속성이 달라집니다.
   > VideoPlayer 에 top, height 속성이 설정되어 있었다면 메소드 실행 후 top, height 속성이 설정됩니다.
   > VideoPlayer 에 top, bottom 속성이 설정되어 있었다면 메소드 실행 후 top, height 속성이 설정됩니다.
   > VideoPlayer 에 height, bottom 속성이 설정되어 있었다면 메소드 실행 후 height, bottom 속성이 설정됩니다.


---

### setOffsetLeft

> Components > Component > VideoPlayer > Method > setOffsetLeft

**Description**

부모 컴포넌트의 Left 위치를 기준으로 VideoPlayer 의 left 값을 픽셀단위로 설정하는 메소드입니다.

**Syntax**

```javascript
VideoPlayer.setOffsetLeft( nLeft );
```

**Parameters**

```
부모 컴포넌트의 Left 위치를 기준으로 VideoPlayer 의 left 값을 픽셀단위의 숫자로 설정합니다
```

**Return**

없음

**Remark**

- 메소드 실행 전 VideoPlayer 에 설정된 초기 속성에 따라 메소드 실행 후 설정되는 속성이 달라집니다.
   > VideoPlayer 에 left, width 속성이 설정되어 있었다면 메소드 실행 후 left, width 속성이 설정됩니다.
   > VideoPlayer 에 left, right 속성이 설정되어 있었다면 메소드 실행 후 left, right 속성이 설정됩니다.
   > VideoPlayer 에 width, right 속성이 설정되어 있었다면 메소드 실행 후 left, width 속성이 설정됩니다.


---

### setOffsetRight

> Components > Component > VideoPlayer > Method > setOffsetRight

**Description**

부모 컴포넌트의 Left 위치를 기준으로 VideoPlayer 의 right 값을 픽셀단위로 설정하는 메소드입니다.

**Syntax**

```javascript
VideoPlayer.setOffsetRight( nRight );
```

**Parameters**

```
부모 컴포넌트의 Left 위치를 기준으로 VideoPlayer 의 right 값을 픽셀단위의 숫자로 설정합니다.
```

**Return**

없음

**Remark**

- 메소드 실행 전 VideoPlayer 에 설정된 초기 속성에 따라 메소드 실행 후 설정되는 속성이 달라집니다.
   > VideoPlayer 에 left, width 속성이 설정되어 있었다면 메소드 실행 후 left, width 속성이 설정됩니다.
   > VideoPlayer 에 left, right 속성이 설정되어 있었다면 메소드 실행 후 left, width 속성이 설정됩니다.
   > VideoPlayer 에 width, right 속성이 설정되어 있었다면 메소드 실행 후 width, right 속성이 설정됩니다.


---

### setOffsetTop

> Components > Component > VideoPlayer > Method > setOffsetTop

**Description**

부모 컴포넌트의 Top 위치를 기준으로 VideoPlayer 의 top 값을 픽셀단위로 설정하는 메소드입니다.

**Syntax**

```javascript
VideoPlayer.setOffsetTop( nTop );
```

**Parameters**

```
부모 컴포넌트의 Top 위치를 기준으로 VideoPlayer 의 top 값을 픽셀단위의 숫자로 설정합니다.
```

**Return**

없음

**Remark**

- 메소드 실행 전 VideoPlayer 에 설정된 초기 속성에 따라 메소드 실행 후 설정되는 속성이 달라집니다.
   > VideoPlayer 에 top, height 속성이 설정되어 있었다면 메소드 실행 후 top, height 속성이 설정됩니다.
   > VideoPlayer 에 top, bottom 속성이 설정되어 있었다면 메소드 실행 후 top, bottom 속성이 설정됩니다.
   > VideoPlayer 에 height, bottom 속성이 설정되어 있었다면 메소드 실행 후 top, height 속성이 설정됩니다.


---

### setOffsetWidth

> Components > Component > VideoPlayer > Method > setOffsetWidth

**Description**

VideoPlayer 의 너비를 픽셀단위로 설정하는 메소드입니다.

**Syntax**

```javascript
VideoPlayer.setOffsetWidth( nWidth );
```

**Parameters**

```
VideoPlayer 의 너비를 픽셀단위의 숫자로 설정합니다.
```

**Return**

없음

**Remark**

- 메소드 실행 전 VideoPlayer 에 설정된 초기 속성에 따라 메소드 실행 후 설정되는 속성이 달라집니다.
   > VideoPlayer 에 left, width 속성이 설정되어 있었다면 메소드 실행 후 left, width 속성이 설정됩니다.
   > VideoPlayer 에 left, right 속성이 설정되어 있었다면 메소드 실행 후 left, width 속성이 설정됩니다.
   > VideoPlayer 에 width, right 속성이 설정되어 있었다면 메소드 실행 후 width, right 속성이 설정됩니다.


---

### show

> Components > Component > VideoPlayer > Method > show

**Description**

스크립트로 동적 생성한 VideoPlayer 을(를) 화면에 표시하는 메소드입니다.

**Syntax**

```javascript
VideoPlayer.show()
```

**Parameters**

this.VideoPlayer00.show();

**Return**

없음

**Remark**

- show() 메소드는 컴포넌트가 폼 또는 컨테이너 컴포넌트의 자식 컴포넌트로 등록된 상태에서 사용이 가능합니다.
  자식 컴포넌트로 등록된 상태가 아닌 경우에는 오동작이 발생할 수 있습니다.

- 컴포넌트를 컨테이너에 추가하기 위해서는 addChild() 메소드나 insertChild() 메소드를 사용하여야 합니다.

**See Also**

Form.addChildForm.insertChildForm.removeChildVideoPlayer.destroyVideoPlayer.init


---

### stop

> Components > Component > VideoPlayer > Method > stop

**Description**

VideoPlayer 에서 재생 중인 비디오를 중지시키고 재생위치를 처음으로 설정하는 메소드입니다.

**Syntax**

```javascript
VideoPlayer.stop();
```

**Return**

없음

**Remark**

- 메소드 수행 시 onplaystatuschanged 이벤트가 발생합니다.


---

### 이벤트 (Events)

### oncurrenttimechanged

> Components > Component > VideoPlayer > Event > oncurrenttimechanged

**Description**

VideoPlayer 에서 비디오 재생위치가 변경되거나 재생중일때 발생하는 이벤트입니다.

**Syntax**

```javascript
oncurrenttimechanged(obj:nexacro.VideoPlayer,e:nexacro.VideoCurrentTimeChangedEventInfo);
```

**Parameters**

```
Event가 발생한 Object.
```

**Return**

없음

**Remark**

- Nexacro Runtime Environment는 currenttime 속성값이 변경되었을 때만 oncurrenttimechanged 이벤트가 발생합니다.

- Web Runtime Environment는 비디오가 재생되는 동안 oncurrenttimechanged 이벤트가 지속적으로 발생합니다.


---

### onerror

> Components > Component > VideoPlayer > Event > onerror

**Description**

VideoPlayer 에서 비디오 파일에 대한 처리가 실패했을 때 발생하는 이벤트입니다.

**Syntax**

```javascript
onerror(obj:nexacro.VideoPlayer,e:nexacro.VideoErrorEventInfo);
```

**Parameters**

```
Event가 발생한 Object.
```

**Return**

없음


---

### onmove

> Components > Component > VideoPlayer > Event > onmove

**Description**

VideoPlayer 의 위치가 이동했을 때 발생하는 이벤트입니다.

**Syntax**

```javascript
onmove(obj:nexacro.VideoPlayer,e:nexacro.MoveEventInfo);
```

**Parameters**

```
Event가 발생한 Object.
```

**Return**

없음


---

### onplaystatuschanged

> Components > Component > VideoPlayer > Event > onplaystatuschanged

**Description**

VideoPlayer 의 재생상태가 변경되었을 때 발생하는 이벤트입니다.

**Syntax**

```javascript
onplaystatuschanged(obj:nexacro.VideoPlayer,e:nexacro.VideoPlayStateChangedEventInfo);
```

**Parameters**

```
Event가 발생한 Object.
```

**Return**

없음

**Remark**

- VideoPlayStateChangedEvenInfo 오브젝트의 newstate 속성에 이벤트가 발생한 원인값이 전달됩니다.

- 재생이 시작되거나 완료되었을 때 onplaystatuschanged 이벤트가 발생합니다.
  기본 UI(ControlBar)를 사용하거나 메소드를 사용하여 재생상태가 변경되었을 때도 onplaystatuschanged 이벤트가 발생합니다.


---

### onsize

> Components > Component > VideoPlayer > Event > onsize

**Description**

VideoPlayer 의 크기가 변경됐을 때 발생하는 이벤트입니다.

**Syntax**

```javascript
onsize(obj:nexacro.VideoPlayer,e:nexacro.SizeEventInfo);
```

**Parameters**

```
Event가 발생한 Object.
```

**Return**

없음


---
